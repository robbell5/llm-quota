---
phase: 01-foreground-tui-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - cmd/llm-quota/main.go
  - internal/tui/model.go
  - internal/tui/update.go
  - internal/tui/update_test.go
autonomous: true
requirements:
  - TUI-01
  - TUI-04
must_haves:
  truths:
    - "User can run `go run ./cmd/llm-quota` and enter a foreground Bubble Tea program per TUI-01 and D-13."
    - "User can quit with `q` or `Ctrl-C` with identical clean-quit behavior per TUI-04, D-05, and D-06."
    - "Unknown arguments fail plainly before TUI startup per D-07 and D-14."
  artifacts:
    - path: "go.mod"
      provides: "Module identity and pinned Charm v2 dependencies per D-15"
      contains: "module github.com/rob/llm-quota"
    - path: "cmd/llm-quota/main.go"
      provides: "Thin executable entrypoint for default TUI launch and argument rejection"
      exports: ["main"]
    - path: "internal/tui/model.go"
      provides: "Bubble Tea model constructor and state shape"
      exports: ["NewModel"]
    - path: "internal/tui/update.go"
      provides: "Init/Update/View methods and clean quit behavior"
      exports: ["Model.Init", "Model.Update", "Model.View"]
    - path: "internal/tui/update_test.go"
      provides: "Automated quit and resize behavior coverage per D-08"
      contains: "TestUpdateQuits"
  key_links:
    - from: "cmd/llm-quota/main.go"
      to: "internal/tui.NewModel"
      via: "tea.NewProgram(tui.NewModel())"
      pattern: "tea\\.NewProgram\\(tui\\.NewModel\\(\\)\\)"
    - from: "internal/tui/update.go"
      to: "Bubble Tea runtime"
      via: "tea.KeyPressMsg q/ctrl+c returns tea.Quit"
      pattern: "case \\"q\\", \\"ctrl\\+c\\""
---

<objective>
Create the compiling Go/Bubble Tea runtime spine for `llm-quota`.

Purpose: Phase 1 must first establish a correct foreground TUI process and quit contract before later phases add data sources, refresh behavior, and richer rendering.
Output: A pinned Go module, a thin `llm-quota` command, a Bubble Tea v2 model/update loop, and automated tests for startup-adjacent behavior.
</objective>

<execution_context>
@/Users/rob/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/rob/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foreground-tui-foundation/01-CONTEXT.md
@.planning/phases/01-foreground-tui-foundation/01-RESEARCH.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@docs/superpowers/specs/2026-05-16-llm-quota-tui-design.md

<interfaces>
No Go source exists yet. This plan creates the first contracts consumed by later plans:

```go
// internal/tui/model.go
type Model struct {
    width  int
    height int
}

func NewModel() Model
```

```go
// internal/tui/update.go
func (m Model) Init() tea.Cmd
func (m Model) Update(msg tea.Msg) (tea.Model, tea.Cmd)
func (m Model) View() tea.View
```

Use Bubble Tea v2 only:

```go
import tea "charm.land/bubbletea/v2"
```

Do not use `github.com/charmbracelet/bubbletea`, `tea.KeyMsg`, or `tea.WithAltScreen()`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin the Go module and create the command entrypoint</name>
  <files>go.mod, go.sum, cmd/llm-quota/main.go</files>
  <read_first>
    - `.planning/phases/01-foreground-tui-foundation/01-CONTEXT.md`
    - `.planning/research/STACK.md`
    - `.planning/research/ARCHITECTURE.md`
    - `docs/superpowers/specs/2026-05-16-llm-quota-tui-design.md`
  </read_first>
  <behavior>
    - Running `go run ./cmd/llm-quota --bogus` exits non-zero and writes a concise error containing `unknown argument: --bogus` before any TUI starts, per D-07 and D-14.
    - Running with no arguments constructs `tea.NewProgram(tui.NewModel())`, per TUI-01 and D-13.
  </behavior>
  <action>Create `go.mod` with module path `github.com/rob/llm-quota` per D-15. Pin dependencies from research: `charm.land/bubbletea/v2` v2.0.6, `charm.land/bubbles/v2` v2.1.0, `charm.land/lipgloss/v2` v2.0.3, and `golang.org/x/sync` v0.20.0. Create `cmd/llm-quota/main.go` as a thin edge: if `len(os.Args) > 1`, print `llm-quota: unknown argument: {arg}` to stderr and `os.Exit(2)`; otherwise run `tea.NewProgram(tui.NewModel())`. If `Run()` returns an error, print `llm-quota: {err}` to stderr and exit 1. Do not add help text, install commands, source readers, manual refresh keys, or future command stubs per D-14.</action>
  <verify>
    <automated>go mod tidy && go test ./...</automated>
    <automated>go run ./cmd/llm-quota --bogus 2&gt;&amp;1 | grep -F "llm-quota: unknown argument: --bogus"</automated>
  </verify>
  <acceptance_criteria>
    - `go.mod` contains `module github.com/rob/llm-quota`.
    - `go.mod` contains `charm.land/bubbletea/v2 v2.0.6`.
    - `go.mod` contains `charm.land/bubbles/v2 v2.1.0`.
    - `go.mod` contains `charm.land/lipgloss/v2 v2.0.3`.
    - `cmd/llm-quota/main.go` contains `tea.NewProgram(tui.NewModel())`.
    - `cmd/llm-quota/main.go` contains `unknown argument:`.
    - `cmd/llm-quota/main.go` does not contain `install-claude-hook`, `install`, or `help` command dispatch.
  </acceptance_criteria>
  <done>No-argument startup enters the Bubble Tea program path, unknown arguments fail plainly, and dependency imports use only the pinned v2 stack.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the initial Bubble Tea model and quit behavior</name>
  <files>internal/tui/model.go, internal/tui/update.go, internal/tui/update_test.go</files>
  <read_first>
    - `internal/tui/model.go` if it exists
    - `internal/tui/update.go` if it exists
    - `.planning/phases/01-foreground-tui-foundation/01-CONTEXT.md`
    - `.planning/phases/01-foreground-tui-foundation/01-RESEARCH.md`
  </read_first>
  <behavior>
    - `Update(tea.KeyPressMsg{Text: "q", Code: 'q'})` returns `tea.Quit` per D-06 and TUI-04.
    - `Update(tea.KeyPressMsg{Code: 'c', Mod: tea.ModCtrl})` returns `tea.Quit` per D-06 and TUI-04.
    - `Update(tea.WindowSizeMsg{Width: 50, Height: 12})` stores width 50 and height 12 for later rendering.
    - `Init()` returns nil because Phase 1 has no source I/O, refresh tick, or setup prompt.
  </behavior>
  <action>Create `internal/tui/model.go` with exported `type Model struct { width int; height int }` and `func NewModel() Model`. Create `internal/tui/update.go` with Bubble Tea v2 methods: `Init() tea.Cmd` returns nil; `Update(msg tea.Msg) (tea.Model, tea.Cmd)` handles `tea.KeyPressMsg` values `q` and `ctrl+c` by returning `tea.Quit`, handles `tea.WindowSizeMsg` by storing `Width` and `Height`, and otherwise returns the unchanged model with nil command; `View() tea.View` returns `tea.NewView(render(m))` with `v.AltScreen = true` per PROJECT key decision and D-05. Include a temporary private `render(m Model) string` in this file returning a non-empty startup string; Plan 02 will replace the renderer with the locked future-shaped screen. Create `internal/tui/update_test.go` with tests named `TestUpdateQuits`, `TestUpdateStoresWindowSize`, and `TestInitHasNoCommand`. Do not handle `r`, source refresh messages, timers, or source errors in this phase per D-09 and phase boundary.</action>
  <verify>
    <automated>go test ./internal/tui -run 'Test(UpdateQuits|UpdateStoresWindowSize|InitHasNoCommand)'</automated>
    <automated>go test ./...</automated>
  </verify>
  <acceptance_criteria>
    - `internal/tui/update.go` contains `case tea.KeyPressMsg:`.
    - `internal/tui/update.go` contains `case "q", "ctrl+c":`.
    - `internal/tui/update.go` contains `return m, tea.Quit`.
    - `internal/tui/update.go` contains `case tea.WindowSizeMsg:`.
    - `internal/tui/update.go` contains `v.AltScreen = true`.
    - `internal/tui/update.go` does not contain `case "r"`.
    - `internal/tui/update_test.go` contains `TestUpdateQuits` and asserts both `q` and `ctrl+c` return a non-nil quit command.
  </acceptance_criteria>
  <done>`q` and `Ctrl-C` quit through the same Bubble Tea command path, terminal dimensions are stored, and Phase 1 contains automated quit coverage per D-08.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shell→CLI args | Untrusted command-line arguments reach `cmd/llm-quota/main.go`. |
| terminal→TUI messages | Terminal keypress and resize messages reach `internal/tui.Update`. |
| dependency resolver→binary | Pinned third-party modules compile into the local foreground binary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Tampering | `cmd/llm-quota/main.go` args | mitigate | Reject every argument except no-argument launch with a concise stderr error and exit code 2, preventing accidental activation of nonexistent setup paths. |
| T-01-01-02 | Denial of Service | `internal/tui.Update` quit handling | mitigate | Handle both `q` and `ctrl+c` through `tea.Quit` and cover them with `TestUpdateQuits` so the foreground pane is always escapable. |
| T-01-01-03 | Tampering | Go dependencies | mitigate | Pin Charm v2 and x/sync versions in `go.mod`; verification runs `go mod tidy` and `go test ./...`. |
| T-01-01-04 | Information Disclosure | startup failure output | accept | Startup errors contain only local program errors and no source data exists in Phase 1. |
</threat_model>

<verification>
Run `go test ./...`. Run `go run ./cmd/llm-quota --bogus` and confirm stderr contains `llm-quota: unknown argument: --bogus`. Do not perform live source-file reads because no source readers exist in this phase.
</verification>

<success_criteria>
- `go.mod` identifies `github.com/rob/llm-quota` and pins the required stack.
- `cmd/llm-quota/main.go` starts `tea.NewProgram(tui.NewModel())` with no arguments.
- Unknown arguments return a plain non-zero failure before startup.
- `internal/tui` handles `q`, `Ctrl-C`, and `WindowSizeMsg` with automated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foreground-tui-foundation/01-01-SUMMARY.md`.
</output>

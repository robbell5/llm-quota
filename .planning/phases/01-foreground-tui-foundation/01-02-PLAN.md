---
phase: 01-foreground-tui-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - internal/tui/update.go
  - internal/tui/view.go
  - internal/tui/colors.go
  - internal/tui/view_test.go
autonomous: false
requirements:
  - TUI-01
  - TUI-04
must_haves:
  truths:
    - "User sees a polished startup screen with title, four placeholder quota rows, and compact footer hints per D-01 through D-04."
    - "User sees only working keys: `q / Ctrl-C quit`, with no `r` hint per D-09 through D-11."
    - "Footer content avoids wrapping at narrow widths by switching to compact copy per D-12."
    - "Maintainer can install and launch the binary path with `go install ./cmd/llm-quota` per D-13 and D-16."
  artifacts:
    - path: "internal/tui/view.go"
      provides: "Phase 1 startup screen renderer with four placeholder rows and compact footer"
      contains: "Claude 5h"
    - path: "internal/tui/colors.go"
      provides: "Local Catppuccin Mocha palette constants for final visual direction per D-03"
      exports: ["mochaBase", "mochaText", "mochaBlue", "mochaYellow", "mochaRed"]
    - path: "internal/tui/view_test.go"
      provides: "Render coverage for placeholder rows, footer key scope, and narrow footer behavior"
      contains: "TestRenderStartupScreen"
  key_links:
    - from: "internal/tui/update.go"
      to: "internal/tui/view.go"
      via: "View calls render(m)"
      pattern: "render\\(m\\)"
    - from: "internal/tui/view.go"
      to: "Phase 1 decisions"
      via: "literal row labels and footer copy"
      pattern: "q / Ctrl-C quit"
---

<objective>
Replace the bare startup renderer with the locked Phase 1 startup screen and final key-hint scope.

Purpose: The first running screen should already resemble the intended quota pane instead of a throwaway title screen, while still avoiding later-phase behavior.
Output: A deterministic renderer, local palette file, render tests, and a human launch/quit checkpoint after automated verification.
</objective>

<execution_context>
@/Users/rob/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/rob/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foreground-tui-foundation/01-CONTEXT.md
@.planning/phases/01-foreground-tui-foundation/01-RESEARCH.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@docs/superpowers/specs/2026-05-16-llm-quota-tui-design.md

<interfaces>
Plan 01 creates these contracts:

```go
type Model struct {
    width  int
    height int
}

func NewModel() Model
func (m Model) Init() tea.Cmd
func (m Model) Update(msg tea.Msg) (tea.Model, tea.Cmd)
func (m Model) View() tea.View
```

This plan must preserve `View() tea.View` and `v.AltScreen = true`. Move only the private renderer out of `update.go` into `view.go`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render the future-shaped startup screen</name>
  <files>internal/tui/update.go, internal/tui/view.go, internal/tui/colors.go, internal/tui/view_test.go</files>
  <read_first>
    - `internal/tui/update.go`
    - `internal/tui/model.go`
    - `.planning/phases/01-foreground-tui-foundation/01-CONTEXT.md`
    - `.planning/research/STACK.md`
    - `docs/superpowers/specs/2026-05-16-llm-quota-tui-design.md`
  </read_first>
  <behavior>
    - Rendered output contains `LLM Quota`.
    - Rendered output contains exactly these four row labels: `Claude 5h`, `Claude 7d`, `Codex 5h`, `Codex 7d`, per D-01.
    - Each placeholder row uses missing-data glyphs/text rather than fake percentages, per D-02.
    - Footer contains `q / Ctrl-C quit` and does not contain `r`, per D-09 and D-10.
    - Narrow width under 50 uses a shorter footer variant that still contains quit copy and avoids source hint wrapping, per D-12.
  </behavior>
  <action>Create `internal/tui/colors.go` with Catppuccin Mocha hex constants copied locally: `mochaBase = lipgloss.Color("#1e1e2e")`, `mochaSurface0 = lipgloss.Color("#313244")`, `mochaText = lipgloss.Color("#cdd6f4")`, `mochaSubtext0 = lipgloss.Color("#a6adc8")`, `mochaBlue = lipgloss.Color("#89b4fa")`, `mochaYellow = lipgloss.Color("#f9e2af")`, `mochaRed = lipgloss.Color("#f38ba8")`. Create `internal/tui/view.go` with private `render(m Model) string`, `renderRows() string`, and `renderFooter(width int) string`. Render a polished terminal shell per D-03: title `LLM Quota`, divider, four aligned rows with labels `Claude 5h`, `Claude 7d`, `Codex 5h`, `Codex 7d`, unavailable placeholders `—` for percent/reset, and hint text for missing local data. Footer at width `>= 50` must be exactly `q / Ctrl-C quit · Claude: install hook · Codex: open Codex`; footer below 50 must be exactly `q / Ctrl-C quit · data pending`. Update `internal/tui/update.go` so `View()` calls `render(m)` from `view.go` and still sets `v.AltScreen = true`. Do not add real quota rows, bars, reset countdown formatting, source readers, timer ticks, or `r` key handling.</action>
  <verify>
    <automated>go test ./internal/tui -run TestRenderStartupScreen</automated>
    <automated>go test ./...</automated>
  </verify>
  <acceptance_criteria>
    - `internal/tui/view.go` contains `LLM Quota`.
    - `internal/tui/view.go` contains `Claude 5h`, `Claude 7d`, `Codex 5h`, and `Codex 7d`.
    - `internal/tui/view.go` contains `q / Ctrl-C quit · Claude: install hook · Codex: open Codex`.
    - `internal/tui/view.go` contains `q / Ctrl-C quit · data pending`.
    - `internal/tui/view.go` does not contain `r refresh`.
    - `internal/tui/view_test.go` contains `TestRenderStartupScreen`.
    - `internal/tui/view_test.go` asserts the rendered startup screen does not contain `r`.
  </acceptance_criteria>
  <done>The startup screen is future-shaped with four placeholder quota rows, concise source hints, the final visual palette direction, and only working key hints.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual launch and quit smoke check</name>
  <files>none</files>
  <what-built>The automated work created a no-argument foreground TUI, clean `q`/`Ctrl-C` quit handling, and the Phase 1 startup screen.</what-built>
  <action>Run the automated verification command, install the binary, and manually launch the TUI twice to verify both quit paths required by D-05, D-06, D-13, and D-16. This checkpoint does not modify files; it records whether real terminal cleanup matches the automated model/update tests.</action>
  <how-to-verify>
    1. Run `go test ./...` and confirm it exits 0.
    2. Run `go install ./cmd/llm-quota` and confirm it exits 0.
    3. Run `go run ./cmd/llm-quota` in a real terminal pane.
    4. Confirm the screen shows `LLM Quota`, four rows (`Claude 5h`, `Claude 7d`, `Codex 5h`, `Codex 7d`), and footer copy `q / Ctrl-C quit` with no `r` key.
    5. Press `q` and confirm the process exits cleanly and returns silently to the shell without a final message or stale frame.
    6. Run `go run ./cmd/llm-quota` again, press `Ctrl-C`, and confirm the same clean quit behavior.
  </how-to-verify>
  <verify>
    <automated>go test ./... && go install ./cmd/llm-quota</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./...` exits 0.
    - `go install ./cmd/llm-quota` exits 0.
    - Manual `q` launch/quit returns to shell without final output.
    - Manual `Ctrl-C` launch/quit returns to shell without panic or traceback.
  </acceptance_criteria>
  <done>Human confirms `go run ./cmd/llm-quota` and the installed `llm-quota` binary launch the startup screen, `q` and `Ctrl-C` both quit cleanly, and no panic, traceback, final message, or stale frame remains.</done>
  <resume-signal>Type `approved` if both quit paths are clean, or describe the exact issue observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model state→terminal output | Renderer converts model dimensions into terminal text and ANSI styling. |
| user terminal→manual smoke check | Human verifies real terminal cleanup that unit tests cannot fully prove. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Information Disclosure | `internal/tui/view.go` | mitigate | Render only static placeholder hints in Phase 1; no home paths, source-file contents, account names, or quota data exist in this plan. |
| T-01-02-02 | Denial of Service | footer/key hints | mitigate | Footer documents only `q / Ctrl-C quit`, and automated tests assert no `r` hint appears before `r` works. |
| T-01-02-03 | Tampering | terminal width handling | mitigate | Use deterministic width branches with render tests for normal and compact footer copy, preventing accidental wrapped footer regressions. |
| T-01-02-04 | Repudiation | manual terminal cleanup | mitigate | Blocking human verification records approval or exact observed issue in the plan execution summary. |
</threat_model>

<verification>
Run `go test ./...`, `go install ./cmd/llm-quota`, then perform the manual launch/quit smoke check for both `q` and `Ctrl-C`.
</verification>

<success_criteria>
- The rendered Phase 1 screen contains the title, four placeholder quota rows, compact data-source hints, and no unavailable key hints.
- `View()` keeps Bubble Tea v2 `tea.View` and alt-screen behavior.
- Automated tests cover render content and key scope.
- Human verification confirms installed binary and `go run` launch paths quit cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foreground-tui-foundation/01-02-SUMMARY.md`.
</output>

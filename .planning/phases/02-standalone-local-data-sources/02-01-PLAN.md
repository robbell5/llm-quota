---
phase: 02-standalone-local-data-sources
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/sources/window.go
  - internal/sources/claude.go
  - internal/sources/claude_test.go
autonomous: true
requirements: [CLD-04, SRC-03, TEST-01]
must_haves:
  truths:
    - "D-09: Claude cache parsing returns two windows only when both cache windows are valid."
    - "D-11: Old but valid Claude cache data returns windows with stale metadata instead of an error."
    - "D-12: Claude source failures are categorized for concise setup/rendering hints."
  artifacts:
    - path: internal/sources/window.go
      provides: "Shared quota window and typed source error contract"
    - path: internal/sources/claude.go
      provides: "Claude cache reader for ~/.cache/llm-quota/claude.json shape"
    - path: internal/sources/claude_test.go
      provides: "Synthetic fixture coverage for Claude cache behavior"
  key_links:
    - from: internal/sources/claude.go
      to: internal/sources/window.go
      via: "Fetch returns []Window and typed SourceError values"
      pattern: "func \(.*\) Fetch\(now time.Time\) \(\[\]Window, error\)"
---

<objective>
Create the normalized source contract and Claude cache reader.

Purpose: Phase 2 needs a standalone, local-only Claude data source that rejects incomplete cache contracts, keeps old valid data usable, and exposes categorized errors for placeholders and footer hints.
Output: `internal/sources` contract files plus Claude reader tests.
</objective>

<execution_context>
@/Users/rob/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/rob/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-standalone-local-data-sources/02-CONTEXT.md
@.planning/phases/02-standalone-local-data-sources/02-RESEARCH.md
@.planning/phases/01-foreground-tui-foundation/01-01-SUMMARY.md
@cmd/llm-quota/main.go

<interfaces>
Existing command edge:
```go
func main() {
	if len(os.Args) > 1 {
		fmt.Fprintf(os.Stderr, "llm-quota: unknown argument: %s\n", os.Args[1])
		os.Exit(2)
	}

	program := tea.NewProgram(tui.NewModel())
	if _, err := program.Run(); err != nil {
		fmt.Fprintf(os.Stderr, "llm-quota: %v\n", err)
		os.Exit(1)
	}
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define source contracts and Claude parser expectations</name>
  <files>internal/sources/window.go, internal/sources/claude_test.go</files>
  <read_first>
    - .planning/phases/02-standalone-local-data-sources/02-CONTEXT.md
    - .planning/phases/02-standalone-local-data-sources/02-RESEARCH.md
    - docs/superpowers/specs/2026-05-16-llm-quota-tui-design.md
  </read_first>
  <behavior>
    - Valid cache with `five_hour`, `seven_day`, and `written_at` returns exactly two `Window` values.
    - Missing cache returns a typed source error with category `missing`.
    - Malformed JSON returns category `malformed`.
    - Cache with only one Claude window is rejected as `malformed` per D-09.
    - Old valid cache returns windows and stale metadata per D-11.
  </behavior>
  <action>Create `internal/sources/window.go` with exported `Product`, `WindowKind`, `Window`, `Metadata`, `SourceError`, and `ErrorCategory` types. Include constants for `ProductClaude`, `ProductCodex`, `WindowFiveHour`, `WindowSevenDay`, `ErrorMissing`, `ErrorMalformed`, `ErrorNoUsableEvent`, and `ErrorRead`. Write `internal/sources/claude_test.go` first with table-driven tests using `t.TempDir()` files; do not use real `~/.cache` paths. The tests must encode the Claude cache contract from D-09 and the research JSON shape: `five_hour.used_percentage`, `five_hour.resets_at`, `seven_day.used_percentage`, `seven_day.resets_at`, and `written_at` as Unix seconds.</action>
  <verify>
    <automated>go test ./internal/sources -run TestClaude</automated>
  </verify>
  <acceptance_criteria>
    - `internal/sources/window.go` contains `type ErrorCategory string`.
    - `internal/sources/window.go` contains `const (` with `ErrorMissing`, `ErrorMalformed`, `ErrorNoUsableEvent`, and `ErrorRead`.
    - `internal/sources/claude_test.go` contains test cases named `valid cache`, `missing cache`, `malformed cache`, `missing seven day rejects all`, and `stale cache returns windows`.
  </acceptance_criteria>
  <done>Tests describe the shared source contract and all required Claude reader behaviors before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Claude cache reader</name>
  <files>internal/sources/window.go, internal/sources/claude.go, internal/sources/claude_test.go</files>
  <read_first>
    - internal/sources/window.go
    - internal/sources/claude_test.go
    - .planning/phases/02-standalone-local-data-sources/02-CONTEXT.md
  </read_first>
  <action>Create `internal/sources/claude.go` with `type ClaudeReader struct`, `func NewClaudeReader(cachePath string) ClaudeReader`, and `func (r ClaudeReader) Fetch(now time.Time) ([]Window, error)`. Read one JSON file only. Map `five_hour` to `WindowFiveHour` label `Claude 5h` and `seven_day` to `WindowSevenDay` label `Claude 7d`. Use `time.Unix(resets_at, 0)` and `time.Unix(written_at, 0)`. Set stale metadata when `now.Sub(writtenAt) > time.Hour` while still returning windows per D-11. If either Claude window is missing, non-numeric, or invalid, return no windows and a malformed typed error per D-09. Wrap `os.IsNotExist` as `ErrorMissing`; other read failures as `ErrorRead`; JSON errors as `ErrorMalformed`. Do not add network, Keychain, statusline, or home-directory default behavior in this package.</action>
  <verify>
    <automated>go test ./internal/sources -run TestClaude</automated>
  </verify>
  <acceptance_criteria>
    - `internal/sources/claude.go` contains `func NewClaudeReader(cachePath string) ClaudeReader`.
    - `internal/sources/claude.go` contains `func (r ClaudeReader) Fetch(now time.Time) ([]Window, error)`.
    - `internal/sources/claude.go` contains `ErrorMissing`, `ErrorMalformed`, and `ErrorRead` mappings.
    - `go test ./internal/sources -run TestClaude` exits 0.
  </acceptance_criteria>
  <done>Claude cache reader passes fixture tests for valid, missing, malformed, incomplete, and stale cache files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local cache file -> source reader | User-writable local JSON crosses into trusted normalized source state. |
| source reader -> TUI hints | Error categories drive user-facing placeholder hints. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | T | `internal/sources/claude.go` | mitigate | Validate both required cache windows and reject partial cache data per D-09. |
| T-02-01-02 | I | `internal/sources/claude.go` | mitigate | Return typed categories without embedding raw cache contents in error strings. |
| T-02-01-03 | D | `internal/sources/claude.go` | mitigate | Convert malformed/missing/read failures into errors; never panic on local file input. |
</threat_model>

<verification>
Run `go test ./internal/sources -run TestClaude` and then `go test ./...` after all Phase 2 source plans complete.
</verification>

<success_criteria>
- Claude cache source behavior covers CLD-04, SRC-03, and TEST-01.
- D-09, D-11, and D-12 are visible in tests and implementation.
- No test reads from the real home directory.
</success_criteria>

<output>
After completion, create `.planning/phases/02-standalone-local-data-sources/02-01-SUMMARY.md`.
</output>

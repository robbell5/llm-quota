---
phase: 02-standalone-local-data-sources
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - internal/sources/codex.go
  - internal/sources/codex_test.go
autonomous: true
requirements: [SRC-01, SRC-02, TEST-02]
must_haves:
  truths:
    - "D-10: Codex parsing skips malformed, unrelated, and null rate-limit lines while searching for usable data."
    - "D-13: A newer unusable rollout file does not prevent fallback to an older usable rollout file."
    - "D-14/D-15: Codex discovery scans all rollout JSONL files and orders them by file modification time."
    - "D-16: Codex plan_type is preserved as optional source metadata."
  artifacts:
    - path: internal/sources/codex.go
      provides: "Codex rollout JSONL reader"
    - path: internal/sources/codex_test.go
      provides: "Synthetic Codex parser and rollout selection tests"
  key_links:
    - from: internal/sources/codex.go
      to: internal/sources/window.go
      via: "Fetch returns normalized Codex 5h and 7d Window values"
      pattern: "ProductCodex"
---

<objective>
Implement the local Codex rollout reader using the normalized source contract.

Purpose: Phase 2 needs reliable local Codex quota extraction from private JSONL rollout files while tolerating noisy session events.
Output: Codex reader and fixture tests.
</objective>

<execution_context>
@/Users/rob/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/rob/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-standalone-local-data-sources/02-CONTEXT.md
@.planning/phases/02-standalone-local-data-sources/02-RESEARCH.md
@internal/sources/window.go
@internal/sources/claude.go

<interfaces>
Downstream contract from Plan 02-01:
```go
type Window struct { /* product, kind, label, percentage, reset, captured/stale metadata */ }
type SourceError struct { /* typed source error with ErrorCategory */ }
func (r ClaudeReader) Fetch(now time.Time) ([]Window, error)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write Codex rollout reader tests</name>
  <files>internal/sources/codex_test.go</files>
  <read_first>
    - internal/sources/window.go
    - .planning/phases/02-standalone-local-data-sources/02-CONTEXT.md
    - .planning/phases/02-standalone-local-data-sources/02-RESEARCH.md
  </read_first>
  <behavior>
    - Newest rollout file with a usable token-count event returns Codex 5h and Codex 7d windows.
    - `rate_limits: null` events are skipped per D-10.
    - Malformed JSONL lines are skipped when another usable line exists per D-10.
    - Newest unusable rollout falls back to an older usable rollout per D-13.
    - No usable rollout returns `ErrorNoUsableEvent` for SRC-02.
  </behavior>
  <action>Create `internal/sources/codex_test.go` using `t.TempDir()` to build a synthetic `sessions` tree. Write rollout files with explicit `os.Chtimes` modification times so the tests prove mtime ordering per D-15. Include JSONL lines for unrelated events, malformed text, `event_msg` token_count with `rate_limits: null`, and a valid `rate_limits` object containing `primary`, `secondary`, and `plan_type`. Assert `primary.window_minutes == 300` maps to `Codex 5h`, `secondary.window_minutes == 10080` maps to `Codex 7d`, and metadata includes `plan_type` per D-16.</action>
  <verify>
    <automated>go test ./internal/sources -run TestCodex</automated>
  </verify>
  <acceptance_criteria>
    - `internal/sources/codex_test.go` contains `os.Chtimes`.
    - `internal/sources/codex_test.go` contains `rate_limits` and `null` in fixture data.
    - `internal/sources/codex_test.go` contains `plan_type` assertions.
    - `internal/sources/codex_test.go` contains a fallback-to-older-rollout test case.
  </acceptance_criteria>
  <done>Codex tests cover SRC-01, SRC-02, TEST-02, and D-10 through D-16 before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Codex rollout reader</name>
  <files>internal/sources/codex.go, internal/sources/codex_test.go</files>
  <read_first>
    - internal/sources/window.go
    - internal/sources/codex_test.go
    - docs/superpowers/specs/2026-05-16-llm-quota-tui-design.md
  </read_first>
  <action>Create `internal/sources/codex.go` with `type CodexReader struct`, `func NewCodexReader(sessionsRoot string) CodexReader`, and `func (r CodexReader) Fetch(now time.Time) ([]Window, error)`. Recursively walk `sessionsRoot` and collect files whose basename starts with `rollout-` and ends with `.jsonl`. Sort candidates by `os.FileInfo.ModTime()` descending. For each candidate, read all content with `os.ReadFile`, split by newline, and scan for the last usable line matching `type == "event_msg"`, `payload.type == "token_count"`, and non-null `payload.rate_limits`. Skip malformed lines and unrelated events. Return the first candidate file that yields usable rate limits. Convert `primary.used_percent` and `primary.resets_at` to `WindowFiveHour` label `Codex 5h`; convert `secondary.used_percent` and `secondary.resets_at` to `WindowSevenDay` label `Codex 7d`. Put `plan_type` in window metadata. If no candidate yields data, return `ErrorNoUsableEvent`; if walking or reading fails because the root is missing, return `ErrorMissing`; permission/read errors return `ErrorRead`.</action>
  <verify>
    <automated>go test ./internal/sources -run TestCodex</automated>
  </verify>
  <acceptance_criteria>
    - `internal/sources/codex.go` contains `func NewCodexReader(sessionsRoot string) CodexReader`.
    - `internal/sources/codex.go` contains `ModTime()` sorting logic.
    - `internal/sources/codex.go` contains `ErrorNoUsableEvent`.
    - `go test ./internal/sources -run TestCodex` exits 0.
  </acceptance_criteria>
  <done>Codex reader passes fixture tests for newest usable data, null/malformed skipping, fallback, no usable event, and metadata preservation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local Codex rollout files -> parser | Private local JSONL session artifacts cross into normalized source state. |
| filesystem tree -> rollout discovery | Arbitrary file names and modification times influence candidate order. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | T | `internal/sources/codex.go` | mitigate | Require exact JSON event predicate and required primary/secondary window fields before returning data. |
| T-02-02-02 | I | `internal/sources/codex.go` | mitigate | Do not include raw rollout payloads in returned errors. |
| T-02-02-03 | D | `internal/sources/codex.go` | mitigate | Skip malformed lines and continue scanning; return categorized errors instead of panicking. |
</threat_model>

<verification>
Run `go test ./internal/sources -run TestCodex` and then `go test ./...` after source reader plans complete.
</verification>

<success_criteria>
- Codex source behavior covers SRC-01, SRC-02, and TEST-02.
- D-10, D-13, D-14, D-15, and D-16 are visible in tests and implementation.
- No test reads real `~/.codex` data.
</success_criteria>

<output>
After completion, create `.planning/phases/02-standalone-local-data-sources/02-02-SUMMARY.md`.
</output>

---
phase: 02-standalone-local-data-sources
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/install/claude_hook.go
  - internal/install/claude_hook_test.go
autonomous: true
requirements: [CLD-01, CLD-02, CLD-03]
must_haves:
  truths:
    - "D-02: A first-launch decline is remembered so normal launches stop prompting repeatedly."
    - "D-05: Only explicitly managed llm-quota hook entries count as app-owned."
    - "D-06/D-07/D-08: Hook installation preserves unrelated config, backs up only before changes, and is idempotent."
  artifacts:
    - path: internal/install/claude_hook.go
      provides: "Claude hook install/update and decline-state policy"
    - path: internal/install/claude_hook_test.go
      provides: "Synthetic config tests for hook safety"
  key_links:
    - from: internal/install/claude_hook.go
      to: cmd/llm-quota/main.go
      via: "Exported install/check functions consumed by CLI wiring in Plan 02-04"
      pattern: "InstallClaudeHook"
---

<objective>
Create the safe, idempotent Claude hook installer package.

Purpose: Phase 2 must let users opt into the app-owned Claude cache producer without overwriting unrelated Claude configuration or pestering users who decline.
Output: `internal/install` package and tests.
</objective>

<execution_context>
@/Users/rob/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/rob/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-standalone-local-data-sources/02-CONTEXT.md
@.planning/phases/02-standalone-local-data-sources/02-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write Claude hook installer safety tests</name>
  <files>internal/install/claude_hook_test.go</files>
  <read_first>
    - .planning/phases/02-standalone-local-data-sources/02-CONTEXT.md
    - .planning/phases/02-standalone-local-data-sources/02-RESEARCH.md
  </read_first>
  <behavior>
    - Managed ownership requires an explicit `llm-quota` marker/name per D-05.
    - Unrelated hook entries are preserved when installing/updating per D-06.
    - A backup is created only when the config file changes per D-07.
    - Re-running install on an already current managed entry reports unchanged per D-08.
    - A declined first-launch prompt is recorded and suppresses repeated prompts per D-02.
  </behavior>
  <action>Create `internal/install/claude_hook_test.go` with table-driven tests over files in `t.TempDir()`. Use synthetic Claude settings JSON with a top-level `hooks` object/array shape chosen in the implementation, but tests must include one unrelated hook entry and one managed entry containing the exact marker string `llm-quota`. Tests must assert unrelated entries survive byte-for-byte or semantically via JSON comparison, backups appear only on changed writes, idempotent second install reports `Changed == false`, and decline state persists in an app-owned state file path passed by the test.</action>
  <verify>
    <automated>go test ./internal/install</automated>
  </verify>
  <acceptance_criteria>
    - `internal/install/claude_hook_test.go` contains `llm-quota` marker fixture data.
    - `internal/install/claude_hook_test.go` contains tests for preserved unrelated hooks, backup on change, no backup when unchanged, and remembered decline.
    - Tests use `t.TempDir()` and do not contain `os.UserHomeDir()`.
  </acceptance_criteria>
  <done>Installer tests encode CLD-01 through CLD-03 and D-02/D-05/D-06/D-07/D-08.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement safe hook install/update policy</name>
  <files>internal/install/claude_hook.go, internal/install/claude_hook_test.go</files>
  <read_first>
    - internal/install/claude_hook_test.go
    - .planning/phases/02-standalone-local-data-sources/02-CONTEXT.md
  </read_first>
  <action>Create `internal/install/claude_hook.go` with exported types/functions that keep policy out of `main.go`: `type ClaudeHookPaths struct { ClaudeConfigPath string; StatePath string; CachePath string }`, `type InstallResult struct { Changed bool; BackupPath string; Message string }`, `func InstallClaudeHook(paths ClaudeHookPaths) (InstallResult, error)`, `func RecordClaudeHookDeclined(statePath string) error`, and `func ClaudeHookDeclined(statePath string) (bool, error)`. Implement config mutation so only entries with marker/name `llm-quota` are updated. Preserve unrelated entries. Before any changed write, copy the existing config to a timestamped sibling backup path; do not create a backup when no write is needed. Use temp-file-plus-rename for writes. The managed hook command may be a small shell invocation that writes the documented cache JSON shape later; the required part in this phase is that the installed entry is identifiable as app-owned and points at the app-owned cache path. Do not implement broader setup aliases or mutate unrelated Claude files.</action>
  <verify>
    <automated>go test ./internal/install</automated>
  </verify>
  <acceptance_criteria>
    - `internal/install/claude_hook.go` contains `func InstallClaudeHook(paths ClaudeHookPaths) (InstallResult, error)`.
    - `internal/install/claude_hook.go` contains `func RecordClaudeHookDeclined(statePath string) error`.
    - `internal/install/claude_hook.go` contains `os.Rename` for atomic writes.
    - `internal/install/claude_hook.go` contains the marker string `llm-quota`.
    - `go test ./internal/install` exits 0.
  </acceptance_criteria>
  <done>Installer preserves unrelated config, manages only marked entries, creates backups only for changed writes, and records declines.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `llm-quota` -> Claude config file | The app writes to user-owned Claude configuration. |
| terminal consent -> file mutation | User permission controls whether install/update writes happen. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | T | `internal/install/claude_hook.go` | mitigate | Update only explicit `llm-quota` managed entries; preserve unrelated hooks. |
| T-02-03-02 | R | `internal/install/claude_hook.go` | mitigate | Return `InstallResult` with changed/unchanged and backup path for user-visible reporting. |
| T-02-03-03 | D | `internal/install/claude_hook.go` | mitigate | Use temp-file-plus-rename and pre-change backups to recover from interrupted writes. |
| T-02-03-04 | E | `internal/install/claude_hook.go` | accept | Local user is already allowed to edit their Claude config; no privilege escalation is introduced. |
</threat_model>

<verification>
Run `go test ./internal/install` and `go test ./...` after CLI wiring.
</verification>

<success_criteria>
- Hook installer covers CLD-01, CLD-02, and CLD-03 policy requirements.
- D-02 and D-05 through D-08 are represented in tests and implementation.
- No code reads or writes real Claude config during tests.
</success_criteria>

<output>
After completion, create `.planning/phases/02-standalone-local-data-sources/02-03-SUMMARY.md`.
</output>

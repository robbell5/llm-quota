---
phase: 02-standalone-local-data-sources
plan: 04
type: execute
wave: 3
depends_on: [02-01, 02-02, 02-03]
files_modified:
  - cmd/llm-quota/main.go
  - cmd/llm-quota/main_test.go
  - internal/tui/view.go
  - internal/tui/view_test.go
autonomous: true
requirements: [CLD-01, CLD-02, CLD-03, SRC-03]
must_haves:
  truths:
    - "D-01: The CLI supports exactly no-arg TUI launch and explicit install-claude-hook setup."
    - "D-03: First-launch permission prompt happens before Bubble Tea alt-screen startup."
    - "D-04: Declining install still launches the TUI with Claude placeholder rows and setup hints."
    - "Existing Phase 1 quit and width behavior still passes."
  artifacts:
    - path: cmd/llm-quota/main.go
      provides: "CLI dispatch and pre-TUI setup prompt wiring"
    - path: cmd/llm-quota/main_test.go
      provides: "Command dispatch tests without entering Bubble Tea"
    - path: internal/tui/view.go
      provides: "Specific placeholder/setup hint copy"
    - path: internal/tui/view_test.go
      provides: "Render regression tests for setup hints"
  key_links:
    - from: cmd/llm-quota/main.go
      to: internal/install/claude_hook.go
      via: "install-claude-hook command and first-launch prompt call installer functions"
      pattern: "install-claude-hook"
    - from: internal/tui/view.go
      to: Phase 2 context D-04
      via: "Claude placeholder rows remain readable after declined setup"
      pattern: "Claude: install hook"
---

<objective>
Wire Phase 2 setup behavior into the command edge and preserve readable placeholder UI.

Purpose: Users must be asked for permission before hook install/update, must be able to explicitly install the app-owned hook, and must still get a usable TUI when they decline or have missing Claude data.
Output: CLI dispatch, pre-alt-screen prompt path, and setup-hint render tests.
</objective>

<execution_context>
@/Users/rob/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/rob/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-standalone-local-data-sources/02-CONTEXT.md
@.planning/phases/02-standalone-local-data-sources/02-RESEARCH.md
@cmd/llm-quota/main.go
@internal/tui/view.go
@internal/tui/view_test.go
@internal/install/claude_hook.go
@internal/sources/window.go

<interfaces>
Existing `main.go` behavior to preserve for unknown arguments:
```go
fmt.Fprintf(os.Stderr, "llm-quota: unknown argument: %s\n", os.Args[1])
os.Exit(2)
```

Existing TUI view contract:
```go
func render(m Model) string
func renderFooter(width int) string
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor command dispatch for testable setup flow</name>
  <files>cmd/llm-quota/main.go, cmd/llm-quota/main_test.go</files>
  <read_first>
    - cmd/llm-quota/main.go
    - internal/install/claude_hook.go
    - .planning/phases/02-standalone-local-data-sources/02-CONTEXT.md
  </read_first>
  <behavior>
    - No args performs first-launch setup check before TUI startup per D-03.
    - `install-claude-hook` runs installer and exits without starting TUI per D-01.
    - Unknown args keep the existing `llm-quota: unknown argument: <arg>` stderr and exit code 2 behavior.
    - Declined setup records decline and still proceeds to TUI startup per D-04.
  </behavior>
  <action>Refactor `cmd/llm-quota/main.go` to introduce a testable `run(args []string, streams, deps) int` or equivalent helper while keeping `main()` tiny. Add dependency injection for installer functions, decline-state functions, stdin/stdout/stderr, and TUI startup so tests never enter Bubble Tea. Implement exactly two command modes in this phase: no arguments and `install-claude-hook`. For no arguments, check whether the app-owned Claude hook is absent and decline state is false; if so, print a plain terminal prompt before `tea.NewProgram(...).Run()` with complete consent copy including `Install llm-quota Claude hook now? [y/N]`. On `y`/`yes`, call `install.InstallClaudeHook`; on any other answer, call `install.RecordClaudeHookDeclined` and then start the TUI. Do not add broader `install`, `setup`, or `help` aliases per D-01.</action>
  <verify>
    <automated>go test ./cmd/llm-quota</automated>
  </verify>
  <acceptance_criteria>
    - `cmd/llm-quota/main.go` contains `install-claude-hook`.
    - `cmd/llm-quota/main.go` contains `Install llm-quota Claude hook now? [y/N]`.
    - `cmd/llm-quota/main_test.go` contains tests for `install-claude-hook`, declined first-launch prompt, accepted first-launch prompt, and unknown argument.
    - `go test ./cmd/llm-quota` exits 0.
  </acceptance_criteria>
  <done>CLI setup behavior is testable, consent happens before TUI startup, explicit install works, and unknown argument behavior is preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve clear placeholder rows and setup hints</name>
  <files>internal/tui/view.go, internal/tui/view_test.go</files>
  <read_first>
    - internal/tui/view.go
    - internal/tui/view_test.go
    - .planning/phases/02-standalone-local-data-sources/02-CONTEXT.md
  </read_first>
  <behavior>
    - Claude placeholder rows remain visible when data is unavailable per CLD-02 and SRC-03.
    - Footer/setup hint clearly says how to install the hook without adding inactive refresh-key copy.
    - Existing width guards still pass at 50, 49, and 29 columns.
  </behavior>
  <action>Update `internal/tui/view.go` placeholder/footer copy only as needed so declined or missing Claude data remains clear. Keep the four existing placeholder rows. Use explicit setup hint copy containing `Claude: run install-claude-hook` when width permits, and keep a compact non-wrapping footer for width 50 and narrower. Do not add quota bars, real data rendering, refresh key hints, last-known-good behavior, or responsive layout polish in this phase. Update `internal/tui/view_test.go` to assert the new setup hint, assert `r refresh` is still absent, and keep line-width assertions for 50, 49, and 29 columns.</action>
  <verify>
    <automated>go test ./internal/tui -run TestRenderStartupScreen</automated>
  </verify>
  <acceptance_criteria>
    - `internal/tui/view.go` contains `Claude: run install-claude-hook`.
    - `internal/tui/view_test.go` asserts `Claude: run install-claude-hook` appears at a wide width.
    - `internal/tui/view_test.go` asserts `r refresh` remains absent.
    - `go test ./internal/tui -run TestRenderStartupScreen` exits 0.
  </acceptance_criteria>
  <done>TUI still runs with readable placeholder rows and setup hints after the user declines hook installation.</done>
</task>

<task type="auto">
  <name>Task 3: Run Phase 2 integration verification</name>
  <files>cmd/llm-quota/main.go, internal/tui/view.go</files>
  <read_first>
    - cmd/llm-quota/main.go
    - internal/tui/view.go
    - internal/sources/claude.go
    - internal/sources/codex.go
    - internal/install/claude_hook.go
  </read_first>
  <action>Run the full Go test suite and command smoke checks. Use only synthetic test paths. Verify that `go test ./...` passes, `go run ./cmd/llm-quota install-claude-hook` reaches the install command path without entering the TUI in test-covered code, and unknown arguments still print the original plain error. If direct `go run ... install-claude-hook` would mutate real Claude config, do not run it against real defaults; rely on injected command tests and document that in the summary.</action>
  <verify>
    <automated>go test ./...</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./...` exits 0.
    - `cmd/llm-quota/main_test.go` covers command dispatch without real home-directory mutation.
    - No test file contains `~/.claude` or `~/.codex` literal paths except in user-facing copy assertions if needed.
  </acceptance_criteria>
  <done>All Phase 2 packages pass together and command wiring does not require unsafe manual setup for verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| terminal user input -> install decision | Plain prompt response controls whether Claude config mutation occurs. |
| CLI args -> command dispatch | User-provided args select TUI, install, or error paths. |
| setup state -> TUI startup | Decline state suppresses prompts but must not block TUI launch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | S | `cmd/llm-quota/main.go` | accept | Local CLI invocation has no identity boundary; no authentication is required. |
| T-02-04-02 | T | `cmd/llm-quota/main.go` | mitigate | Dispatch only exact `install-claude-hook`; unknown args preserve explicit error path. |
| T-02-04-03 | R | `cmd/llm-quota/main.go` | mitigate | Print changed/unchanged install result messages from `InstallResult`. |
| T-02-04-04 | D | `internal/tui/view.go` | mitigate | Declined setup still starts TUI and renders placeholders; no source error can crash startup. |
</threat_model>

<verification>
Run `go test ./cmd/llm-quota`, `go test ./internal/tui -run TestRenderStartupScreen`, and `go test ./...`.
</verification>

<success_criteria>
- CLD-01, CLD-02, CLD-03, and SRC-03 are represented in command and render tests.
- D-01, D-03, and D-04 are implemented without adding out-of-scope aliases or refresh behavior.
- Phase 1 launch/quit/render tests continue to pass.
</success_criteria>

<output>
After completion, create `.planning/phases/02-standalone-local-data-sources/02-04-SUMMARY.md`.
</output>

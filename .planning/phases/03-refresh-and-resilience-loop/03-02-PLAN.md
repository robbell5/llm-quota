---
phase: 03-refresh-and-resilience-loop
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - cmd/llm-quota/main.go
  - cmd/llm-quota/main_test.go
  - internal/tui/view.go
  - internal/tui/view_test.go
autonomous: true
requirements: [SRC-04, SRC-05, TUI-02, TUI-03]
must_haves:
  truths:
    - "D-13/TUI-02: Real no-arg TUI startup wires Claude and Codex readers so Init can refresh local data immediately and every 30 seconds."
    - "D-03/SRC-05: Valid stale data remains renderable model data instead of reverting to placeholders."
    - "D-04: Visible stale warning copy and styling are not finalized in Phase 3; rendering only preserves stale-but-valid data availability."
    - "D-06/SRC-04: If no successful refresh exists, the existing placeholder rows and hints remain available."
    - "D-08: Temporary source failure warning prominence is deferred to Phase 4 while Phase 3 keeps rows stable."
    - "D-16/UI-SPEC: Phase 3 does not add visible refreshing status, last-updated copy, final stale age text, or a visible r-refresh footer hint."
  artifacts:
    - path: "cmd/llm-quota/main.go"
      provides: "Real Claude cache and Codex sessions readers wired into tui.NewModel"
      contains: "sources.NewClaudeReader"
    - path: "cmd/llm-quota/main_test.go"
      provides: "Command-edge regression test proving no-arg startup constructs a source-backed model without entering real Bubble Tea in tests"
      contains: "StartTUI"
    - path: "internal/tui/view.go"
      provides: "Minimal source-backed row rendering while preserving Phase 3 copy boundaries"
      contains: "renderRows"
    - path: "internal/tui/view_test.go"
      provides: "Render regression tests for source-backed rows and forbidden Phase 3 copy"
      contains: "TestRender"
  key_links:
    - from: "cmd/llm-quota/main.go"
      to: "internal/sources/claude.go"
      via: "default cache path wired to sources.NewClaudeReader"
      pattern: "sources\.NewClaudeReader"
    - from: "cmd/llm-quota/main.go"
      to: "internal/sources/codex.go"
      via: "default sessions root wired to sources.NewCodexReader"
      pattern: "sources\.NewCodexReader"
    - from: "internal/tui/view.go"
      to: "internal/tui/model.go"
      via: "rows read normalized windows from Model state"
      pattern: "windows"
---

<objective>
Wire the refresh state machine to real local readers and keep the visible TUI
within the Phase 3 design contract.

Purpose: Running `llm-quota` should use the Phase 2 Claude and Codex readers, and
the screen should remain stable: placeholders before data, minimal quota values
when data exists, and no Phase 4 warning/status polish yet.
Output: Real reader wiring at the command edge plus render tests that prove data
rows do not regress placeholder behavior or forbidden copy boundaries.
</objective>

<execution_context>
@/Users/rob/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/rob/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-refresh-and-resilience-loop/03-CONTEXT.md
@.planning/phases/03-refresh-and-resilience-loop/03-RESEARCH.md
@.planning/phases/03-refresh-and-resilience-loop/03-UI-SPEC.md
@.planning/phases/03-refresh-and-resilience-loop/03-PATTERNS.md
@.planning/phases/02-standalone-local-data-sources/02-04-SUMMARY.md
@.planning/phases/02-standalone-local-data-sources/02-05-SUMMARY.md
@cmd/llm-quota/main.go
@cmd/llm-quota/main_test.go
@internal/tui/view.go
@internal/tui/view_test.go
@internal/sources/claude.go
@internal/sources/codex.go

<interfaces>
Created by Plan 03-01 and used here:

```go
func NewModel(opts ...Option) Model
func WithReaders(claude SourceReader, codex SourceReader) Option
```

Existing source constructors:

```go
func NewClaudeReader(cachePath string) ClaudeReader
func NewCodexReader(sessionsRoot string) CodexReader
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire real source readers at the command edge</name>
  <files>cmd/llm-quota/main.go, cmd/llm-quota/main_test.go</files>
  <read_first>cmd/llm-quota/main.go, cmd/llm-quota/main_test.go, internal/sources/claude.go, internal/sources/codex.go, internal/tui/model.go, .planning/phases/03-refresh-and-resilience-loop/03-CONTEXT.md</read_first>
  <behavior>
    - No-arg startup still runs the first-launch Claude hook offer before starting Bubble Tea.
    - Real TUI startup constructs `tui.NewModel(tui.WithReaders(...))` with Claude cache path and Codex sessions root.
    - Existing explicit commands `install-claude-hook` and `claude-hook-cache-writer --cache <path>` keep their current behavior.
  </behavior>
  <action>Update `cmd/llm-quota/main.go` to import `github.com/rob/llm-quota/internal/sources`. Extend the default path edge so `startTUI` can construct `sources.NewClaudeReader(filepath.Join(home, ".cache", "llm-quota", "claude.json"))` and `sources.NewCodexReader(filepath.Join(home, ".codex", "sessions"))`, then pass both into `tui.NewModel(tui.WithReaders(claudeReader, codexReader))`. Keep source path lookup in `main.go`; do not move home-directory defaults into `internal/tui` or `internal/sources`. Preserve the first-launch setup prompt order from Phase 2. Add or update command-edge tests so no test starts a real Bubble Tea program or reads real home-directory data.</action>
  <verify>
    <automated>go test ./cmd/llm-quota -run 'TestRun|TestStart|TestInstall|TestClaudeHook'</automated>
  </verify>
  <acceptance_criteria>
    - `cmd/llm-quota/main.go` contains `internal/sources`.
    - `cmd/llm-quota/main.go` contains `sources.NewClaudeReader`.
    - `cmd/llm-quota/main.go` contains `sources.NewCodexReader`.
    - `cmd/llm-quota/main.go` contains `.codex` and `sessions` in default path construction.
    - `cmd/llm-quota/main_test.go` contains a no-real-TUI startup test using injected dependencies or a test seam.
    - `go test ./cmd/llm-quota -run 'TestRun|TestStart|TestInstall|TestClaudeHook'` exits zero.
  </acceptance_criteria>
  <done>No-arg app startup uses source-backed TUI defaults while command dispatch and setup prompt behavior remain intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render minimal source-backed rows without Phase 4 polish</name>
  <files>internal/tui/view.go, internal/tui/view_test.go</files>
  <read_first>internal/tui/view.go, internal/tui/view_test.go, internal/tui/model.go, internal/sources/window.go, .planning/phases/03-refresh-and-resilience-loop/03-UI-SPEC.md</read_first>
  <behavior>
    - Empty model state still renders the four existing placeholder-capable rows.
    - Model state with windows renders the corresponding `UsedPercent` values and reset placeholders/countdown-compatible text without adding progress bars.
    - Stale-but-valid windows are still rendered as data, not placeholders, per D-03.
    - Render output does not contain `refreshing`, `last updated`, final stale age text, or visible `r refresh` copy per D-16 and UI-SPEC.
  </behavior>
  <action>Update `renderRows` so it accepts the current `Model` and can render normalized windows by product/kind. Keep the existing labels `Claude 5h`, `Claude 7d`, `Codex 5h`, and `Codex 7d`. For available windows, render a simple percent string such as `42%` and reset text derived from `ResetsAt`; do not add Bubbles progress bars, threshold colors, final stale/error copy, `refreshing...`, `last updated`, or footer `r refresh` copy. For missing windows, preserve current placeholder behavior: wide rows include `— missing local data reset —`, compact rows include `— pending`, and very narrow rows show short label plus `—`. Add tests that construct `Model` state directly or through package-private helpers with synthetic `sources.Window` values.</action>
  <verify>
    <automated>go test ./internal/tui -run 'TestRender'</automated>
    <automated>go test ./internal/tui</automated>
  </verify>
  <acceptance_criteria>
    - `internal/tui/view.go` contains `renderRows(m Model` or another signature that passes `Model` into row rendering.
    - `internal/tui/view_test.go` contains an assertion for `42%` or another literal synthetic percentage from a `sources.Window` fixture.
    - `internal/tui/view_test.go` contains assertions that `refreshing`, `last updated`, and `r refresh` are absent.
    - `internal/tui/view_test.go` still contains width guard coverage for widths `50`, `49`, and `29`.
    - `go test ./internal/tui` exits zero.
  </acceptance_criteria>
  <done>Rendering can show refreshed quota values in the existing row structure while preserving placeholder fallback and Phase 3 copy boundaries.</done>
</task>

<task type="auto">
  <name>Task 3: Run phase verification and guard against real-data coupling</name>
  <files>cmd/llm-quota/main.go, cmd/llm-quota/main_test.go, internal/tui/view.go, internal/tui/view_test.go</files>
  <read_first>cmd/llm-quota/main.go, cmd/llm-quota/main_test.go, internal/tui/update_test.go, internal/tui/view_test.go, go.mod</read_first>
  <action>Run formatting and the full Go test suite. Then scan test files to ensure no test reads real `~/.claude`, `~/.codex`, or `.cache/llm-quota` paths outside expected literal path-construction assertions. If a test needs paths, use `t.TempDir()` or injected strings only.</action>
  <verify>
    <automated>go fmt ./cmd/llm-quota ./internal/tui</automated>
    <automated>go test ./cmd/llm-quota ./internal/tui</automated>
    <automated>go test ./...</automated>
    <automated>rg '~/(\.claude|\.codex)|os\.UserHomeDir\(' cmd internal --glob '*_test.go'</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./...` exits zero.
    - Any `os.UserHomeDir(` occurrence in tests is limited to existing default-path tests that do not read from the returned directory.
    - Test files contain no real home-directory source reads.
  </acceptance_criteria>
  <done>Phase 3 source wiring and rendering pass focused and full-suite verification without real local data coupling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| command defaults → source readers | Real user-local paths are passed to readers at runtime but must not be used in tests. |
| model state → renderer | Renderer consumes normalized source data and typed errors without parsing local files. |
| terminal width → renderer | Width values are untrusted and must not produce wrapped or overflowing rows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Information Disclosure | `cmd/llm-quota/main_test.go` | mitigate | Use injected dependencies and temp paths; never read real Claude/Codex data in tests. |
| T-03-06 | Tampering | `cmd/llm-quota/main.go` | mitigate | Keep command dispatch unchanged for installer/cache-writer paths; only wire readers for no-arg TUI startup. |
| T-03-07 | Denial of Service | `internal/tui/view.go` | mitigate | Preserve width guards for 50, 49, and 29 columns so rendering does not overflow small panes. |
| T-03-08 | Spoofing | source-backed rows | accept | Phase 3 displays normalized local data only; provenance/warning presentation is deferred to Phase 4 by UI-SPEC. |
</threat_model>

<verification>
Run:

```bash
go fmt ./cmd/llm-quota ./internal/tui
go test ./cmd/llm-quota ./internal/tui
go test ./...
```
</verification>

<success_criteria>
- Real no-arg TUI startup uses Claude and Codex local readers.
- Empty state still renders placeholders and setup hints.
- Refreshed source windows can render as simple row values without Phase 4 bars,
  thresholds, or final warning copy.
- Phase 3 forbidden visible copy remains absent.
- Full Go test suite passes.
</success_criteria>

<output>
After completion, create `.planning/phases/03-refresh-and-resilience-loop/03-refresh-and-resilience-loop-02-SUMMARY.md`.
</output>

---
phase: 04-quota-display-and-responsive-rendering
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - internal/tui/view.go
  - internal/tui/view_test.go
autonomous: true
requirements:
  - DISP-01
  - DISP-02
  - DISP-03
  - DISP-04
  - DISP-05
  - DISP-06
  - TUI-05
  - TUI-06
  - TEST-04
must_haves:
  truths:
    - "D-02/D-03/TUI-05/TUI-06: User can resize to widths 50, 49, 30, and 29 without row wrapping; below 30 columns bars are omitted but status remains readable."
    - "D-09/D-10: Missing source rows show concise user-facing recovery copy, never raw internal error categories."
    - "D-11/D-12: Stale-but-valid rows render normally and footer hints prioritize the most actionable recovery/setup message that fits."
  artifacts:
    - path: internal/tui/view.go
      provides: "Responsive data/missing row layouts and footer hint selection"
      contains: "renderFooter(m Model"
    - path: internal/tui/view_test.go
      provides: "Render coverage for missing-source, stale-source, and narrow-width states"
      contains: "TestRenderResponsiveQuotaLayouts"
  key_links:
    - from: internal/tui/view.go
      to: internal/tui/model.go
      via: "footer derives missing and stale hints from `Model.errors` and `Model.windows`"
      pattern: "m\.errors|StaleAge|Stale"
    - from: internal/tui/view_test.go
      to: internal/tui/view.go
      via: "ANSI-stripped line width assertions at breakpoint widths"
      pattern: "assertRenderedLineWidths"
---

<!-- markdownlint-disable MD013 MD060 -->

<objective>
Complete the resilient and responsive dashboard behavior.

Purpose: The final display must remain useful when data is missing/stale and when the tmux pane is narrower than the target 50-column layout.
Output: Tested missing-source rows, stale footer hints, source-hint priority, and no-wrap responsive layouts.
</objective>

<execution_context>
@/Users/rob/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/rob/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-quota-display-and-responsive-rendering/04-CONTEXT.md
@.planning/phases/04-quota-display-and-responsive-rendering/04-UI-SPEC.md
@.planning/phases/04-quota-display-and-responsive-rendering/04-RESEARCH.md
@.planning/phases/04-quota-display-and-responsive-rendering/04-PATTERNS.md
@.planning/phases/04-quota-display-and-responsive-rendering/04-01-SUMMARY.md
@internal/tui/view.go
@internal/tui/view_test.go
@internal/tui/model.go
@internal/sources/window.go

<interfaces>
```go
// internal/tui/model.go
type Model struct {
    width  int
    height int
    now     func() time.Time
    windows map[sources.Product][]sources.Window
    errors  map[sources.Product]sources.SourceError
}

// internal/sources/window.go
type SourceError struct {
    Source   Product
    Category ErrorCategory
    Err      error
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify missing, stale, footer, and narrow layout behavior</name>
  <files>internal/tui/view_test.go</files>
  <read_first>
    - internal/tui/view_test.go
    - internal/tui/view.go
    - internal/tui/model.go
    - internal/sources/window.go
    - .planning/phases/04-quota-display-and-responsive-rendering/04-UI-SPEC.md
  </read_first>
  <behavior>
    - Missing Claude rows with `sources.ErrorMissing` show `Claude: run install-claude-hook` in the footer when it fits.
    - Missing Codex rows with `sources.ErrorNoUsableEvent` show `Codex: open Codex` in the footer when no higher-priority Claude setup hint applies.
    - Raw error category strings `malformed`, `read_error`, and `no_usable_event` never appear in ANSI-stripped output.
    - Stale-but-valid Claude or Codex rows keep percent/progress output and add footer copy like `Claude data 2h old; open Claude` when no missing-source hint has priority.
    - Widths 50, 49, 30, 29, and 20 produce no ANSI-stripped line wider than the terminal width; below 30 columns no progress bar glyph run is required.
  </behavior>
  <action>
    Replace the Phase 3 copy-boundary expectations in `TestRenderSourceBackedRowsWithoutPhaseFourCopy` with Phase 4 expectations, then add `TestRenderMissingAndStaleFooterHints` and `TestRenderResponsiveQuotaLayouts`. Use synthetic `Model` values only. In missing-source tests, populate `model.errors[sources.ProductClaude] = sources.SourceError{Source: sources.ProductClaude, Category: sources.ErrorMissing}` and assert the plain render contains `Claude: run install-claude-hook`. Populate a separate model with Codex missing/error and no Claude error, then assert `Codex: open Codex`. In stale tests, use a valid `sources.Window{Stale: true, StaleAge: 2 * time.Hour}` and assert the row still contains its percent plus footer copy `Claude data 2h old; open Claude`. For narrow layout tests, render a four-window model at widths `50`, `49`, `30`, `29`, and `20`, call `assertRenderedLineWidths` for each, assert width 49 uses abbreviated labels `Cl 5h` and `Cx 7d`, and assert width 29 contains the percent text but no wide full label `Claude 5h`.</action>
  <verify>
    <automated>go test ./internal/tui -run 'TestRender(MissingAndStaleFooterHints|ResponsiveQuotaLayouts|SourceBackedRows)' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `internal/tui/view_test.go` contains `func TestRenderMissingAndStaleFooterHints`.
    - `internal/tui/view_test.go` contains `func TestRenderResponsiveQuotaLayouts`.
    - `internal/tui/view_test.go` contains literal strings `Claude: run install-claude-hook`, `Codex: open Codex`, and `Claude data 2h old; open Claude`.
    - `internal/tui/view_test.go` asserts that `malformed`, `read_error`, and `no_usable_event` are absent from plain output.
    - `go test ./internal/tui -run 'TestRender(MissingAndStaleFooterHints|ResponsiveQuotaLayouts|SourceBackedRows)' -count=1` fails before Task 2 implementation and passes after Task 2.
  </acceptance_criteria>
  <done>Tests define the missing-source, stale-source, footer priority, and responsive breakpoint behavior required by D-02, D-03, and D-09 through D-12.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement footer priority and responsive row degradation</name>
  <files>internal/tui/view.go, internal/tui/view_test.go</files>
  <read_first>
    - internal/tui/view.go
    - internal/tui/view_test.go
    - internal/tui/model.go
    - internal/sources/window.go
    - .planning/phases/04-quota-display-and-responsive-rendering/04-CONTEXT.md
    - .planning/phases/04-quota-display-and-responsive-rendering/04-UI-SPEC.md
  </read_first>
  <action>
    Implement D-02, D-03, D-09, D-10, D-11, and D-12 in `internal/tui/view.go`. Change the footer call from `renderFooter(width)` to `renderFooter(m, innerWidth)`. Add footer helper behavior in this priority order: missing Claude data with no Claude windows returns `Claude: run install-claude-hook`; missing Codex data with no Codex windows returns `Codex: open Codex`; stale Claude rows return `Claude data {age} old; open Claude`; stale Codex rows return `Codex data {age} old; open Codex`; baseline returns `q / Ctrl-C quit` and appends ` · r refresh` only when it fits. Build the footer by appending hints only while `lipgloss.Width(candidate) <= innerWidth`; do not rotate hints. Add an `ageText(time.Duration) string` helper that emits `2h` for two hours, `45m` for under an hour, and `1d` or greater day counts for day-scale stale ages. Update missing rows so width `>= 50` shows full label plus `— missing local data reset —`, width `30-49` uses abbreviated labels such as `Cl 5h` plus `— pending`, and width `< 30` uses short label plus `—`. Update data rows so width `30-49` abbreviates labels before shrinking the bar, and width `< 30` omits the progress bar while preserving short label, percent, and reset token when it fits. Never expose `SourceError.Category` strings in user-facing output.</action>
  <verify>
    <automated>go test ./internal/tui -run TestRender -count=1</automated>
    <automated>go test ./internal/tui -count=1</automated>
    <automated>go test ./... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `internal/tui/view.go` contains `func renderFooter(m Model, innerWidth int) string`.
    - `internal/tui/view.go` contains `Claude: run install-claude-hook`, `Codex: open Codex`, `q / Ctrl-C quit`, and `r refresh`.
    - `internal/tui/view.go` contains `ageText` and reads `StaleAge` or `Stale` when building footer hints.
    - `internal/tui/view.go` contains width branches for `>= 50`, `>= 30`, and `< 30` row layouts.
    - `go test ./... -count=1` exits 0.
  </acceptance_criteria>
  <done>Rows and footer degrade in the required order, source failures remain source-specific, stale data stays visible with bounded footer hints, and all tested widths stay within terminal bounds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `SourceError` → footer copy | Internal local error categories cross into user-facing rendering and must be mapped to safe, actionable copy. |
| terminal width → row layout | External terminal resize messages affect row composition and can cause wrapping if not bounded. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Information Disclosure | `renderFooter` | mitigate | Map all `SourceError.Category` values to fixed user-facing hints and test raw category strings are absent. |
| T-04-02-02 | Denial of Service | responsive row assembly | mitigate | Test widths 50, 49, 30, 29, and 20 with ANSI-stripped line width assertions. |
| T-04-02-03 | Repudiation | stale data display | mitigate | Footer names stale source and age unit so the user can distinguish current from stale values while rows remain visible. |
</threat_model>

<verification>
Run these checks after both tasks:

```bash
go test ./internal/tui -run TestRender -count=1
go test ./internal/tui -count=1
go test ./... -count=1
```
</verification>

<success_criteria>
- Widths 50, 49, 30, 29, and 20 render without ANSI-stripped line overflow.
- Missing Claude and Codex data produce actionable footer hints without raw parser category names.
- Stale-but-valid rows keep their percent/progress display and add source/age footer copy when no missing-source hint has priority.
- The full Go test suite passes with synthetic render tests only.
</success_criteria>

<output>
After completion, create `.planning/phases/04-quota-display-and-responsive-rendering/04-02-SUMMARY.md`.
</output>

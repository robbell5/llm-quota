---
phase: 05-install-docs-and-real-pane-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - DOC-01
  - DOC-02
must_haves:
  truths:
    - "DOC-01/D-01/D-03: User can install with go install and then run llm-quota install-claude-hook from README instructions."
    - "DOC-02/D-09/D-10/D-11/D-12: User can map visible Claude, Codex, and stale-data hints to concrete recovery actions without raw internal error category names."
    - "D-02: Phase 5 documentation does not require new CLI help, setup aliases, network fallback, statusline integration, or demo modes."
  artifacts:
    - path: README.md
      provides: "Primary user-facing install, setup, run, keys, tmux-pane, and troubleshooting documentation"
      contains: "llm-quota install-claude-hook"
  key_links:
    - from: README.md
      to: cmd/llm-quota/main.go
      via: "documented command surface"
      pattern: "install-claude-hook"
    - from: README.md
      to: internal/tui/view.go
      via: "troubleshooting copy mirrors footer hints"
      pattern: "Claude: run install-claude-hook|Claude: open Claude|Codex: open Codex"
---

<!-- markdownlint-disable MD013 -->

<objective>
Create the primary user-facing README for installing, setting up, running, and troubleshooting `llm-quota` as a standalone tmux-pane tool.

Purpose: Phase 5 must make the completed local-only TUI usable without relying on Rob's custom statusline or unstated local knowledge.

Output: `README.md` with quickstart, install, standalone Claude hook setup, run keys, tmux-pane expectations, local source paths, and troubleshooting that mirrors the TUI footer hints.
</objective>

<execution_context>
@/Users/rob/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/rob/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-install-docs-and-real-pane-validation/05-CONTEXT.md
@.planning/phases/05-install-docs-and-real-pane-validation/05-RESEARCH.md
@cmd/llm-quota/main.go
@internal/tui/view.go

<interfaces>
From `cmd/llm-quota/main.go`:

```go
func run(args []string, streams appStreams, deps appDeps) int
// Supported public commands: no args for TUI launch, install-claude-hook for explicit setup.
// Internal command: claude-hook-cache-writer --cache <path>.
```

From `internal/tui/view.go`:

```go
const (
    quitHint          = "q / Ctrl-C quit"
    refreshHint       = "r refresh"
    claudeInstallHint = "Claude: run install-claude-hook"
    claudeOpenHint    = "Claude: open Claude"
    codexOpenHint     = "Codex: open Codex"
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create README quickstart and operating guide</name>
  <files>README.md</files>
  <read_first>
    - README.md if it exists; otherwise confirm it is absent before creating it.
    - .planning/phases/05-install-docs-and-real-pane-validation/05-CONTEXT.md
    - cmd/llm-quota/main.go
    - internal/tui/view.go
  </read_first>
  <action>Create `README.md` as the primary user-facing documentation per D-05 and D-06. Include these exact sections: `# llm-quota`, `## What it shows`, `## Install`, `## Set up Claude quota data`, `## Run in a tmux pane`, `## Keys`, `## Troubleshooting`, and `## Scope`. Under `## Install`, document `go install github.com/rob/llm-quota/cmd/llm-quota@latest` as the primary v1 path per D-01, and include a local verification alternative using `go build ./cmd/llm-quota` per D-04. Under `## Set up Claude quota data`, document the explicit command `llm-quota install-claude-hook` per D-03, explain that it installs or updates only the app-owned Claude hook/cache writer, and state that the hook writes local quota data to `~/.cache/llm-quota/claude.json` after Claude runs per D-07/D-10. Under `## Run in a tmux pane`, describe `llm-quota` as an always-running foreground TUI intended for a small dedicated pane and mention the 30-second refresh cadence. Under `## Keys`, include `r` for manual refresh and `q` / `Ctrl-C` for quit. Under `## Troubleshooting`, mirror the user-facing TUI hints: `Claude: run install-claude-hook`, `Claude: open Claude`, `Codex: open Codex`, and stale-data age hints; explain Codex local data comes from opening Codex so rollout data appears under `~/.codex/sessions` per D-11. Under `## Scope`, explicitly say v1 does not use network/OAuth fallback, Keychain reads, statusline integration, a daemon, alerts, forecasting, demo mode, or fixture mode per D-02 and D-15.</action>
  <verify>
    <automated>test -f README.md &amp;&amp; grep -q 'go install github.com/rob/llm-quota/cmd/llm-quota@latest' README.md &amp;&amp; grep -q 'llm-quota install-claude-hook' README.md &amp;&amp; grep -q 'Claude: run install-claude-hook' README.md &amp;&amp; grep -q 'Codex: open Codex' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `## Install`, `## Set up Claude quota data`, `## Run in a tmux pane`, `## Keys`, `## Troubleshooting`, and `## Scope`.
    - `README.md` contains `go install github.com/rob/llm-quota/cmd/llm-quota@latest`.
    - `README.md` contains `llm-quota install-claude-hook`.
    - `README.md` contains `~/.cache/llm-quota/claude.json` and `~/.codex/sessions`.
    - `README.md` contains `Claude: run install-claude-hook`, `Claude: open Claude`, and `Codex: open Codex`.
  </acceptance_criteria>
  <done>README gives a new user the install, setup, run, key, pane, and troubleshooting path without adding or depending on new CLI commands.</done>
</task>

<task type="auto">
  <name>Task 2: Verify README against command surface and documentation boundaries</name>
  <files>README.md</files>
  <read_first>
    - README.md
    - cmd/llm-quota/main.go
    - .planning/REQUIREMENTS.md
    - .planning/phases/05-install-docs-and-real-pane-validation/05-CONTEXT.md
  </read_first>
  <action>Run focused documentation and code smoke checks. Verify that README references only the public command surface implemented in `cmd/llm-quota/main.go`: no-arg `llm-quota` and `llm-quota install-claude-hook`. Do not document `claude-hook-cache-writer` as a user-facing command. Confirm README avoids raw internal source error category names by checking that the tokens `ErrorMissing`, `ErrorMalformed`, `ErrorRead`, `ErrorNoUsableEvent`, `missing_local_data`, `malformed`, `read_error`, and `no_usable_event` are absent from non-code-fence user prose. Run `go test ./... -count=1` to verify the binary and tests still pass after documentation-only changes.</action>
  <verify>
    <automated>go test ./... -count=1</automated>
    <automated>! grep -E 'ErrorMissing|ErrorMalformed|ErrorRead|ErrorNoUsableEvent|missing_local_data|read_error|no_usable_event' README.md</automated>
    <automated>grep -q '^## Scope' README.md &amp;&amp; grep -q 'statusline' README.md &amp;&amp; grep -q 'network' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./... -count=1` exits 0.
    - README does not contain `ErrorMissing`, `ErrorMalformed`, `ErrorRead`, `ErrorNoUsableEvent`, `missing_local_data`, `read_error`, or `no_usable_event`.
    - README contains the word `statusline` in the Scope section to preserve the no-statusline boundary.
    - README does not introduce `--help`, `help`, `install`, demo mode, fixture mode, network fallback, OAuth fallback, Keychain reads, daemon mode, alerts, or forecasting as Phase 5 capabilities.
  </acceptance_criteria>
  <done>README is consistent with implemented commands, local-only boundaries, and DOC-01/DOC-02 troubleshooting requirements.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README reader → local shell | User copies documented install/setup commands into a shell. |
| README troubleshooting → private local files | User follows guidance involving Claude/Codex local paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | `README.md` install/setup commands | mitigate | Document only `go install github.com/rob/llm-quota/cmd/llm-quota@latest` and `llm-quota install-claude-hook`; do not add undocumented aliases or shell mutation steps. |
| T-05-02 | Information Disclosure | `README.md` troubleshooting | mitigate | Mention user-visible paths only and avoid private Claude/Codex schema details or raw source-error category names. |
| T-05-03 | Repudiation | Documentation traceability | mitigate | Tie README content to DOC-01/DOC-02 and D-01 through D-12 in this plan and verify with grep gates. |
</threat_model>

<verification>
- `go test ./... -count=1` passes.
- README contains the install, setup, run, keys, tmux-pane, troubleshooting, and scope sections.
- README mirrors user-facing footer hints and avoids raw internal error categories.
</verification>

<success_criteria>
- DOC-01 is satisfied by documented `go install`, `llm-quota install-claude-hook`, and `llm-quota` run instructions.
- DOC-02 is satisfied by troubleshooting copy for missing Claude, missing Codex, and stale-but-valid local data.
- D-01 through D-12 are explicitly implemented or preserved without adding out-of-scope CLI behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/05-install-docs-and-real-pane-validation/05-01-SUMMARY.md`.
</output>

---
phase: 05-install-docs-and-real-pane-validation
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - .planning/phases/05-install-docs-and-real-pane-validation/05-HUMAN-UAT.md
autonomous: false
requirements:
  - DOC-01
  - DOC-02
must_haves:
  truths:
    - "D-13/D-14: Real tmux-pane validation is captured in a Phase 5 validation artifact, not in README release evidence."
    - "D-16: Manual validation covers default refresh cadence, quit keys, responsive widths 50/49/30/29, and perceived green/yellow/red terminal colors."
    - "User can validate install, standalone Claude hook setup, troubleshooting hints, and final pane ergonomics before release."
  artifacts:
    - path: .planning/phases/05-install-docs-and-real-pane-validation/05-HUMAN-UAT.md
      provides: "Manual real-pane validation checklist and result record"
      contains: "widths 50, 49, 30, and 29"
  key_links:
    - from: .planning/phases/05-install-docs-and-real-pane-validation/05-HUMAN-UAT.md
      to: .planning/phases/04-quota-display-and-responsive-rendering/04-HUMAN-UAT.md
      via: "carries forward pending Phase 4 human UAT checks"
      pattern: "Tmux pane visual layout check|Terminal color perception check"
    - from: .planning/phases/05-install-docs-and-real-pane-validation/05-HUMAN-UAT.md
      to: README.md
      via: "validates documented install/setup/run path"
      pattern: "go install|install-claude-hook|tmux"
---

<!-- markdownlint-disable MD013 -->

<objective>
Create and execute the Phase 5 real-pane validation record for the installed TUI experience.

Purpose: The project cannot claim the tmux-pane release is usable until a human validates the real terminal behavior that automated ANSI-stripped tests cannot prove.

Output: `.planning/phases/05-install-docs-and-real-pane-validation/05-HUMAN-UAT.md` with a checklist and recorded results for install smoke, standalone Claude setup, troubleshooting hints, refresh/quit behavior, responsive widths, and perceived terminal colors.
</objective>

<execution_context>
@/Users/rob/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/rob/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@README.md
@.planning/phases/05-install-docs-and-real-pane-validation/05-CONTEXT.md
@.planning/phases/04-quota-display-and-responsive-rendering/04-VERIFICATION.md
@.planning/phases/04-quota-display-and-responsive-rendering/04-HUMAN-UAT.md
@cmd/llm-quota/main.go
@internal/tui/view.go

<interfaces>
Carry-forward human checks from Phase 4:

```text
Open the TUI in its intended tmux pane and visually inspect widths 50, 49, 30, and 29.
Inspect green/yellow/red quota urgency colors in a color-capable terminal.
```

Phase 5 adds install/setup validation:

```text
Verify README install path, explicit llm-quota install-claude-hook setup path, 30-second cadence, r refresh, q/Ctrl-C quit, and troubleshooting hints.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 5 human validation artifact</name>
  <files>.planning/phases/05-install-docs-and-real-pane-validation/05-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/05-install-docs-and-real-pane-validation/05-CONTEXT.md
    - .planning/phases/04-quota-display-and-responsive-rendering/04-VERIFICATION.md
    - .planning/phases/04-quota-display-and-responsive-rendering/04-HUMAN-UAT.md
    - README.md
  </read_first>
  <action>Create `.planning/phases/05-install-docs-and-real-pane-validation/05-HUMAN-UAT.md` per D-13 and D-14. Include frontmatter with `status: pending`, `phase: 05-install-docs-and-real-pane-validation`, `source: [05-01-PLAN.md, 05-02-PLAN.md, 04-HUMAN-UAT.md]`, `started: <current ISO timestamp>`, and `updated: <current ISO timestamp>`. Include sections `# Phase 05 Human UAT`, `## How to run`, `## Tests`, `## Results`, and `## Summary`. Under `## How to run`, instruct the human to run the app in a dedicated tmux pane with the locally built or installed `llm-quota` binary, use README setup instructions, and avoid demo/fixture mode per D-15. Under `## Tests`, include numbered checklist items for: install smoke through README command/build output per D-04, explicit `llm-quota install-claude-hook` setup per D-03, visible troubleshooting hints matching README and TUI footer copy per D-09 through D-12, default 30-second refresh cadence, `r` manual refresh, `q` quit, `Ctrl-C` quit, responsive widths 50/49/30/29, and green/yellow/red color perception per D-16. Initialize each result as `pending`.</action>
  <verify>
    <automated>test -f .planning/phases/05-install-docs-and-real-pane-validation/05-HUMAN-UAT.md &amp;&amp; grep -q 'widths 50, 49, 30, and 29' .planning/phases/05-install-docs-and-real-pane-validation/05-HUMAN-UAT.md &amp;&amp; grep -q 'green/yellow/red' .planning/phases/05-install-docs-and-real-pane-validation/05-HUMAN-UAT.md &amp;&amp; grep -q 'llm-quota install-claude-hook' .planning/phases/05-install-docs-and-real-pane-validation/05-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `05-HUMAN-UAT.md` contains `status: pending` in frontmatter before human validation.
    - `05-HUMAN-UAT.md` contains `llm-quota install-claude-hook`.
    - `05-HUMAN-UAT.md` contains `default 30-second refresh cadence`.
    - `05-HUMAN-UAT.md` contains `q quit` and `Ctrl-C quit`.
    - `05-HUMAN-UAT.md` contains `widths 50, 49, 30, and 29`.
    - `05-HUMAN-UAT.md` contains `green/yellow/red`.
  </acceptance_criteria>
  <done>Phase 5 has a concrete manual validation artifact that carries forward Phase 4 human-needed checks and adds install/setup/troubleshooting release checks.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Validate the finished app in a real tmux pane</name>
  <what-built>The README from Plan 01 and the `05-HUMAN-UAT.md` checklist from Task 1 are ready. Automated Go tests must pass before requesting human validation.</what-built>
  <files>.planning/phases/05-install-docs-and-real-pane-validation/05-HUMAN-UAT.md</files>
  <read_first>
    - README.md
    - .planning/phases/05-install-docs-and-real-pane-validation/05-HUMAN-UAT.md
    - .planning/phases/04-quota-display-and-responsive-rendering/04-HUMAN-UAT.md
  </read_first>
  <action>Before pausing, run `go test ./... -count=1` and `go build ./cmd/llm-quota`. Then ask the human to follow `05-HUMAN-UAT.md` in a real dedicated tmux pane. The human should validate README install/setup instructions without requiring broader PATH mutation, run `llm-quota install-claude-hook`, start `llm-quota`, observe the default 30-second refresh cadence, press `r`, quit with `q`, restart and quit with `Ctrl-C`, resize/inspect widths 50, 49, 30, and 29, and inspect perceived green/yellow/red terminal colors. After the human responds with approved or issues, update `05-HUMAN-UAT.md`: change frontmatter `status` to `passed` if approved or `issues` if problems were reported; update each checklist `result` from `pending` to `passed`, `failed`, or `blocked`; update the Summary counts; and include any user-described issues verbatim in a `## Issues` section. Do not add screenshots, demo mode, fixture mode, or public validation mode per D-13/D-15.</action>
  <how-to-verify>
    1. Run `go test ./... -count=1`; expected: exit 0.
    2. Run `go build ./cmd/llm-quota`; expected: exit 0.
    3. Open a real dedicated tmux pane and run the locally built or installed `llm-quota` binary.
    4. Follow the checklist in `.planning/phases/05-install-docs-and-real-pane-validation/05-HUMAN-UAT.md`.
    5. Confirm rows remain readable at widths 50, 49, 30, and 29.
    6. Confirm perceived low/yellow/red quota colors are acceptable in the actual terminal.
    7. Confirm `r`, `q`, and `Ctrl-C` behave as documented.
  </how-to-verify>
  <verify>
    <automated>go test ./... -count=1</automated>
    <automated>go build ./cmd/llm-quota</automated>
    <automated>grep -q '^status: passed\|^status: issues' .planning/phases/05-install-docs-and-real-pane-validation/05-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./... -count=1` exits 0 before human validation.
    - `go build ./cmd/llm-quota` exits 0 before human validation.
    - Human validation response is recorded in `05-HUMAN-UAT.md`.
    - `05-HUMAN-UAT.md` frontmatter is no longer `status: pending` after the checkpoint resolves.
    - `05-HUMAN-UAT.md` Summary counts match the individual test results.
  </acceptance_criteria>
  <resume-signal>Type "approved" after completing the real-pane checklist, or describe issues to record in `05-HUMAN-UAT.md`.</resume-signal>
  <done>Real tmux-pane validation is recorded, including any issues, so Phase 5 can close the remaining human-needed release checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human validator → planning artifact | Human observations are recorded as release validation evidence. |
| Local shell/tmux → app runtime | Validation commands run the real local binary in a terminal environment. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Repudiation | `05-HUMAN-UAT.md` validation record | mitigate | Record each checklist result and any issues in the artifact instead of claiming validation from memory. |
| T-05-05 | Tampering | Real-pane validation method | mitigate | Prohibit demo/fixture/public validation modes and validate the real binary path described by README. |
| T-05-06 | Information Disclosure | Human-reported local source issues | mitigate | Record symptoms and user-facing hints only; do not request or paste private Claude/Codex JSON payloads. |
</threat_model>

<verification>
- `go test ./... -count=1` passes.
- `go build ./cmd/llm-quota` passes.
- `05-HUMAN-UAT.md` exists and contains the required Phase 5 and Phase 4 carry-forward checks.
- Human checkpoint result is recorded in `05-HUMAN-UAT.md`.
</verification>

<success_criteria>
- DOC-01 is validated through install/build and standalone Claude hook setup checks.
- DOC-02 is validated through missing/stale data troubleshooting checks that mirror README and TUI footer hints.
- D-13 through D-16 are implemented by a manual, real-pane validation artifact with no demo/fixture mode.
</success_criteria>

<output>
After completion, create `.planning/phases/05-install-docs-and-real-pane-validation/05-02-SUMMARY.md`.
</output>

---
phase: 06-i-think-we-are-missing-an-uninstaller
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/install/claude_hook.go
  - internal/install/claude_hook_test.go
  - cmd/llm-quota/main.go
  - cmd/llm-quota/main_test.go
autonomous: true
requirements: [CLD-03]
must_haves:
  truths:
    - "User can run `llm-quota uninstall-claude-hook` to remove only app-owned Claude setup."
    - "Existing non-llm-quota Claude hooks and statusline commands survive uninstall."
    - "A previously wrapped statusline command is restored after uninstall."
  artifacts:
    - path: "internal/install/claude_hook.go"
      provides: "UninstallClaudeHook implementation and ownership-preserving Claude settings mutation"
      contains: "func UninstallClaudeHook(paths ClaudeHookPaths) (InstallResult, error)"
    - path: "cmd/llm-quota/main.go"
      provides: "Public uninstall-claude-hook command dispatch"
      contains: "case \"uninstall-claude-hook\""
    - path: "internal/install/claude_hook_test.go"
      provides: "Synthetic tests for uninstall safety and reversibility"
      contains: "TestUninstallClaudeHook"
    - path: "cmd/llm-quota/main_test.go"
      provides: "CLI dispatch tests for uninstall-claude-hook"
      contains: "TestRunUninstallClaudeHook"
  key_links:
    - from: "cmd/llm-quota/main.go"
      to: "internal/install.UninstallClaudeHook"
      via: "run command dispatch dependency injection"
      pattern: "UninstallClaudeHook"
    - from: "internal/install/claude_hook.go"
      to: "~/.claude/settings.json"
      via: "ClaudeHookPaths.ClaudeConfigPath and writeJSONAtomic"
      pattern: "writeJSONAtomic"
---

<objective>
Add a safe Claude setup uninstaller command.

Purpose: Phase 5 made installation usable, but users also need a reversible way to remove the app-owned Claude integration without damaging unrelated Claude configuration.
Output: A tested `llm-quota uninstall-claude-hook` command that removes only `llm-quota`-owned Claude settings and restores any wrapped user statusline command.
</objective>

<execution_context>
@/Users/rob/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/rob/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-i-think-we-are-missing-an-uninstaller/06-RESEARCH.md
@.planning/phases/05-install-docs-and-real-pane-validation/05-02-SUMMARY.md
@internal/install/claude_hook.go
@internal/install/claude_hook_test.go
@cmd/llm-quota/main.go
@cmd/llm-quota/main_test.go

<interfaces>
From `internal/install/claude_hook.go`:
```go
type ClaudeHookPaths struct {
	ClaudeConfigPath string
	StatePath        string
	CachePath        string
	ExecutablePath   string
}

type InstallResult struct {
	Changed    bool
	BackupPath string
	Message    string
}

func InstallClaudeHook(paths ClaudeHookPaths) (InstallResult, error)
func ManagedStatusLineCommand(executablePath string, cachePath string, passthrough string) string
```

From `cmd/llm-quota/main.go`:
```go
type appDeps struct {
	Paths                    func() (install.ClaudeHookPaths, error)
	ClaudeHookInstalled      func(install.ClaudeHookPaths) (bool, error)
	ClaudeHookDeclined       func(string) (bool, error)
	RecordClaudeHookDeclined func(string) error
	InstallClaudeHook        func(install.ClaudeHookPaths) (install.InstallResult, error)
	CodexSessionsRoot        func() (string, error)
	StartTUI                 func(tui.Model) error
}

func run(args []string, streams appStreams, deps appDeps) int
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add uninstall primitive for app-owned Claude setup</name>
  <files>internal/install/claude_hook.go, internal/install/claude_hook_test.go</files>
  <read_first>internal/install/claude_hook.go; internal/install/claude_hook_test.go; .planning/phases/06-i-think-we-are-missing-an-uninstaller/06-RESEARCH.md</read_first>
  <behavior>
    - Test 1: managed `statusLine` with `llm_quota_marker: "llm-quota"` and `llm_quota_passthrough: "statusline.sh"` is changed to `statusLine: {"type":"command","command":"statusline.sh"}`.
    - Test 2: managed `statusLine` with empty passthrough is removed from the config.
    - Test 3: managed old `hooks.PostToolUse` entries with `llm_quota_marker: "llm-quota"` are removed while unrelated hook entries remain.
    - Test 4: markerless or unrelated `statusLine` and hook values are unchanged and produce no backup.
    - Test 5: symlinked Claude settings remain symlinked after uninstall writes through to the target.
  </behavior>
  <action>Create `func UninstallClaudeHook(paths ClaudeHookPaths) (InstallResult, error)` in `internal/install/claude_hook.go`. Return an error when `ClaudeConfigPath` is empty. Read the existing config with `readClaudeConfig`; if the file is absent or empty, return `InstallResult{Message: "llm-quota Claude hook is not installed"}` with `Changed: false`. Implement marker-only removal: if `config["statusLine"]` is a `map[string]any` whose `llm_quota_marker` equals `managedHookMarker`, read `llm_quota_passthrough` as a string. If passthrough is non-empty, set `config["statusLine"] = map[string]any{"type": "command", "command": passthrough}`; if passthrough is empty, delete `config["statusLine"]`. Always call `removeManagedToolHook(config)` so older app-owned PostToolUse entries are cleaned up. Compare with the cloned original map; if unchanged, return the not-installed message without backup. If changed and the file existed, create a backup using `backupFile(paths.ClaudeConfigPath)`, then persist with `writeJSONAtomic(paths.ClaudeConfigPath, config)` to preserve symlink behavior. Return `InstallResult{Changed: true, BackupPath: backupPath, Message: "uninstalled llm-quota Claude hook"}`. Do not delete `paths.CachePath` or `paths.StatePath`; this command only removes Claude settings integration.</action>
  <verify>
    <automated>go test ./internal/install -run 'TestUninstallClaudeHook' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `internal/install/claude_hook.go` contains `func UninstallClaudeHook(paths ClaudeHookPaths) (InstallResult, error)`.
    - `internal/install/claude_hook_test.go` contains `TestUninstallClaudeHookRestoresWrappedStatusLine`.
    - `internal/install/claude_hook_test.go` contains `TestUninstallClaudeHookRemovesManagedStatusLineWithoutPassthrough`.
    - `internal/install/claude_hook_test.go` contains `TestUninstallClaudeHookRemovesOldManagedToolHookAndPreservesUnrelatedHooks`.
    - `internal/install/claude_hook_test.go` contains `TestUninstallClaudeHookLeavesUnmanagedConfigUnchanged`.
    - `go test ./internal/install -run 'TestUninstallClaudeHook' -count=1` exits 0.
  </acceptance_criteria>
  <done>Uninstall logic is marker-scoped, restores passthrough statusline commands, preserves unrelated config, preserves symlinks, and is covered by focused install-package tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire uninstall-claude-hook into the CLI</name>
  <files>cmd/llm-quota/main.go, cmd/llm-quota/main_test.go</files>
  <read_first>cmd/llm-quota/main.go; cmd/llm-quota/main_test.go; internal/install/claude_hook.go</read_first>
  <behavior>
    - Test 1: `run([]string{"uninstall-claude-hook"}, ...)` calls the injected uninstaller, prints `uninstalled llm-quota Claude hook`, returns exit code 0, and does not start the TUI.
    - Test 2: `run([]string{"uninstall-claude-hook", "extra"}, ...)` returns exit code 2, prints `llm-quota: unknown argument: extra`, and does not call the uninstaller or start the TUI.
  </behavior>
  <action>Extend `appDeps` with `UninstallClaudeHook func(install.ClaudeHookPaths) (install.InstallResult, error)`. In `withDefaults`, set it to `install.UninstallClaudeHook` when nil. Add `case "uninstall-claude-hook"` in `run` next to `install-claude-hook`; reject extra args with the same `llm-quota: unknown argument: %s\n` pattern and exit code 2. Create `runUninstallClaudeHook(streams appStreams, deps appDeps) int` mirroring `runInstallClaudeHook`: resolve paths via `deps.Paths()`, call `deps.UninstallClaudeHook(paths)`, print `result.Message`, print `backup: <path>` when `BackupPath` is non-empty, return 1 on path/uninstall errors, and never start Bubble Tea. Keep existing internal commands unchanged.</action>
  <verify>
    <automated>go test ./cmd/llm-quota -run 'TestRunUninstallClaudeHook|TestRunUnknownArgumentPreservesErrorAndExitCode' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `cmd/llm-quota/main.go` contains `case "uninstall-claude-hook"`.
    - `cmd/llm-quota/main.go` contains `UninstallClaudeHook func(install.ClaudeHookPaths) (install.InstallResult, error)`.
    - `cmd/llm-quota/main.go` contains `func runUninstallClaudeHook(streams appStreams, deps appDeps) int`.
    - `cmd/llm-quota/main_test.go` contains `TestRunUninstallClaudeHookCommandUninstallsWithoutStartingTUI`.
    - `cmd/llm-quota/main_test.go` contains `TestRunUninstallClaudeHookRejectsExtraArgs`.
    - `go test ./cmd/llm-quota -run 'TestRunUninstallClaudeHook|TestRunUnknownArgumentPreservesErrorAndExitCode' -count=1` exits 0.
  </acceptance_criteria>
  <done>The public uninstall command dispatches through dependency injection, prints deterministic output, rejects extra arguments, and cannot accidentally launch the TUI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local Claude settings JSON → uninstaller | User-controlled JSON is parsed, transformed, backed up, and rewritten. |
| CLI args → command dispatcher | User-provided command arguments choose whether settings are mutated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | `internal/install.UninstallClaudeHook` | mitigate | Remove only entries with `llm_quota_marker == "llm-quota"`; add tests proving unrelated hooks/statusline survive. |
| T-06-02 | Repudiation | settings rewrite backup | mitigate | Reuse `backupFile` on changed existing config and print `backup: <path>` from the CLI. |
| T-06-03 | Denial of Service | statusline restoration | mitigate | Restore non-empty `llm_quota_passthrough` to a plain command statusline instead of deleting the user's original command. |
| T-06-04 | Elevation of Privilege | passthrough command | accept | Uninstall restores a pre-existing local user command string but does not execute it. |
</threat_model>

<verification>
Run `go test ./internal/install ./cmd/llm-quota -count=1` after both tasks.
</verification>

<success_criteria>
- `llm-quota uninstall-claude-hook` is a supported public command.
- App-owned Claude statusline wrapper and old managed tool hooks can be removed safely.
- Unrelated Claude config is preserved, and wrapped statusline commands are restored.
- Focused command and installer tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/06-i-think-we-are-missing-an-uninstaller/06-01-SUMMARY.md`.
</output>

---
phase: 06-i-think-we-are-missing-an-uninstaller
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - README.md
  - .planning/phases/06-i-think-we-are-missing-an-uninstaller/06-HUMAN-UAT.md
autonomous: false
requirements: [DOC-01, DOC-02]
must_haves:
  truths:
    - "User can find the uninstall command in the README near Claude setup instructions."
    - "User understands uninstall removes app-owned Claude integration but leaves local cache files intact."
    - "Maintainer has a release validation record showing install → uninstall → reinstall remains reversible."
  artifacts:
    - path: "README.md"
      provides: "User-facing uninstall instructions and troubleshooting copy"
      contains: "llm-quota uninstall-claude-hook"
    - path: ".planning/phases/06-i-think-we-are-missing-an-uninstaller/06-HUMAN-UAT.md"
      provides: "Manual validation checklist for uninstall/reinstall safety"
      contains: "uninstall-claude-hook"
  key_links:
    - from: "README.md"
      to: "cmd/llm-quota/main.go"
      via: "documented public command"
      pattern: "uninstall-claude-hook"
---

<objective>
Document and validate the Claude setup uninstaller.

Purpose: The uninstall command is only useful if users can discover it and if the release record proves it is safe in the same real-local setup style as Phase 5.
Output: README uninstall instructions plus a Phase 6 human validation artifact for install/uninstall/reinstall safety.
</objective>

<execution_context>
@/Users/rob/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/rob/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-i-think-we-are-missing-an-uninstaller/06-RESEARCH.md
@.planning/phases/05-install-docs-and-real-pane-validation/05-02-SUMMARY.md
@README.md
@cmd/llm-quota/main.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document uninstall usage and boundaries</name>
  <files>README.md</files>
  <read_first>README.md; cmd/llm-quota/main.go; .planning/phases/06-i-think-we-are-missing-an-uninstaller/06-RESEARCH.md</read_first>
  <action>Update `README.md` so the Claude setup section includes an uninstall subsection. Add exact installed-path command `llm-quota uninstall-claude-hook` and local-build command `./llm-quota uninstall-claude-hook`. State that uninstall removes the app-owned Claude statusline cache writer from `~/.claude/settings.json`, restores the previously wrapped statusline command when one was present, preserves unrelated Claude configuration, and does not delete `~/.cache/llm-quota/claude.json` or `~/.cache/llm-quota/state.json`. In Troubleshooting, add one sentence that rerunning `llm-quota install-claude-hook` re-enables Claude quota capture after uninstall. Do not document the internal `claude-hook-cache-writer` or `claude-statusline-cache-writer` commands as user-facing commands.</action>
  <verify>
    <automated>grep -q 'llm-quota uninstall-claude-hook' README.md && grep -q './llm-quota uninstall-claude-hook' README.md && grep -q 'does not delete' README.md && ! grep -q '^.*claude-statusline-cache-writer' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `llm-quota uninstall-claude-hook`.
    - `README.md` contains `./llm-quota uninstall-claude-hook`.
    - `README.md` contains `does not delete ~/.cache/llm-quota/claude.json or ~/.cache/llm-quota/state.json`.
    - `README.md` contains `restores the previously wrapped statusline command`.
    - `README.md` does not expose `claude-statusline-cache-writer` as a standalone user command.
  </acceptance_criteria>
  <done>README users can discover how to uninstall, what is removed, what is preserved, and how to reinstall.</done>
</task>

<task type="auto">
  <name>Task 2: Create uninstall validation checklist</name>
  <files>.planning/phases/06-i-think-we-are-missing-an-uninstaller/06-HUMAN-UAT.md</files>
  <read_first>.planning/phases/05-install-docs-and-real-pane-validation/05-HUMAN-UAT.md; README.md; cmd/llm-quota/main.go</read_first>
  <action>Create `.planning/phases/06-i-think-we-are-missing-an-uninstaller/06-HUMAN-UAT.md` with frontmatter `phase: 06-i-think-we-are-missing-an-uninstaller`, `status: pending`, and `validated_at: null`. Include a checklist with these exact validation steps: build local binary with `go build ./cmd/llm-quota`; run `./llm-quota install-claude-hook`; confirm Claude settings contain an app-owned `llm_quota_marker`; run `./llm-quota uninstall-claude-hook`; confirm the app-owned marker is absent; confirm any previous statusline command is restored when present; confirm `~/.cache/llm-quota/claude.json` is not deleted by uninstall; rerun `./llm-quota install-claude-hook`; run `go test ./... -count=1`. Leave result checkboxes unchecked for human validation.</action>
  <verify>
    <automated>test -f .planning/phases/06-i-think-we-are-missing-an-uninstaller/06-HUMAN-UAT.md && grep -q 'status: pending' .planning/phases/06-i-think-we-are-missing-an-uninstaller/06-HUMAN-UAT.md && grep -q './llm-quota uninstall-claude-hook' .planning/phases/06-i-think-we-are-missing-an-uninstaller/06-HUMAN-UAT.md && grep -q 'go test ./... -count=1' .planning/phases/06-i-think-we-are-missing-an-uninstaller/06-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `06-HUMAN-UAT.md` contains `status: pending`.
    - `06-HUMAN-UAT.md` contains `./llm-quota install-claude-hook`.
    - `06-HUMAN-UAT.md` contains `./llm-quota uninstall-claude-hook`.
    - `06-HUMAN-UAT.md` contains `llm_quota_marker`.
    - `06-HUMAN-UAT.md` contains `~/.cache/llm-quota/claude.json is not deleted`.
    - `06-HUMAN-UAT.md` contains `go test ./... -count=1`.
  </acceptance_criteria>
  <done>Phase 6 has a concrete validation artifact for the real install/uninstall/reinstall flow.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify uninstall in a real local Claude setup</name>
  <files>.planning/phases/06-i-think-we-are-missing-an-uninstaller/06-HUMAN-UAT.md</files>
  <action>Pause after the automated documentation and checklist work. The human validator must follow the generated UAT checklist and update `06-HUMAN-UAT.md` with the real local result before Phase 6 is considered complete.</action>
  <what-built>The agent has added README uninstall docs and a pending real-local uninstall validation checklist.</what-built>
  <how-to-verify>
    1. Build the local binary from the repository root: `go build ./cmd/llm-quota`.
    2. Follow `.planning/phases/06-i-think-we-are-missing-an-uninstaller/06-HUMAN-UAT.md` using the local `./llm-quota` commands.
    3. Confirm uninstall removes only the app-owned Claude integration and reinstall restores quota capture setup.
    4. Update `06-HUMAN-UAT.md` from `status: pending` to `status: passed` with the validation result.
  </how-to-verify>
  <verify>
    <automated>grep -q '^status: passed' .planning/phases/06-i-think-we-are-missing-an-uninstaller/06-HUMAN-UAT.md && ! grep -q '^status: pending' .planning/phases/06-i-think-we-are-missing-an-uninstaller/06-HUMAN-UAT.md</automated>
  </verify>
  <done>`06-HUMAN-UAT.md` records `status: passed` after real-local uninstall/reinstall validation.</done>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README instructions → user's shell | User may copy commands that mutate local Claude settings. |
| human validation → phase approval | Manual validation records whether real local config behavior matches automated tests. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Tampering | README uninstall instructions | mitigate | Document exact command behavior: removes app-owned Claude settings integration, restores passthrough, preserves unrelated config. |
| T-06-06 | Repudiation | `06-HUMAN-UAT.md` | mitigate | Require pending checklist to be updated to `status: passed` only after real local validation. |
| T-06-07 | Information Disclosure | docs and UAT | mitigate | Do not ask the validator to paste or commit private Claude settings contents; only record marker presence/absence and command outcomes. |
</threat_model>

<verification>
Run `go test ./... -count=1` and the README/UAT grep checks from the tasks after Plan 06-01 is complete.
</verification>

<success_criteria>
- README documents installed and local-build uninstall commands.
- README states the uninstaller preserves unrelated Claude config and leaves local cache/state files in place.
- Phase 6 UAT artifact exists with pending real-local uninstall/reinstall validation steps.
- Human checkpoint records approved real-local validation before Phase 6 is considered complete.
</success_criteria>

<output>
After completion, create `.planning/phases/06-i-think-we-are-missing-an-uninstaller/06-02-SUMMARY.md`.
</output>

---
phase: 07-row-alignment-claude-sonnet-limit-and-source-freshness
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/sources/window.go
  - internal/sources/claude.go
  - internal/sources/claude_test.go
  - internal/install/claude_hook.go
  - internal/install/claude_hook_test.go
  - internal/tui/view.go
  - internal/tui/view_test.go
autonomous: true
requirements:
  - CLD-05
  - CLD-06
  - POL-01
  - POL-03
user_setup: []
must_haves:
  truths:
    - "D-01: Startup and missing-Claude-Sonnet states render a visible `Sonnet 7d` placeholder row instead of omitting the row."
    - "D-02: Normal-width Claude Sonnet row label is `Sonnet 7d`; compact layouts may use `Sn 7d`."
    - "D-03: Claude rows render in this order: `Claude 5h`, `Claude 7d`, `Sonnet 7d`."
    - "D-04: Missing or malformed optional Sonnet weekly cache data does not reject required `five_hour` and `seven_day` Claude windows."
    - "D-05: Normal-width quota rows use fixed percent and reset columns so `0h 54m`, `21h 1m`, and `100%` align vertically."
    - "D-06: Normal reset text may include minutes; compact reset text drops minutes to hours where needed; very narrow rows may omit reset text."
    - "D-07: Progress bars remain visible until they would crowd label, percent, and reset text; text readability wins over bar visibility."
    - "D-08: The `Sonnet 7d` placeholder row reserves the same label, bar/marker, percent, and reset columns as real quota rows."
  artifacts:
    - path: internal/sources/window.go
      provides: "WindowSonnetSevenDay shared window kind"
      contains: "WindowSonnetSevenDay"
    - path: internal/sources/claude.go
      provides: "Optional Sonnet weekly Claude cache parsing"
      contains: "sonnet_seven_day"
    - path: internal/install/claude_hook.go
      provides: "Optional Sonnet weekly Claude cache writing"
      contains: "SonnetSevenDay"
    - path: internal/tui/view.go
      provides: "Ordered Sonnet row and fixed-column quota row layout"
      contains: "Sonnet 7d"
  key_links:
    - from: internal/install/claude_hook.go
      to: internal/sources/claude.go
      via: "canonical optional cache key"
      pattern: "sonnet_seven_day"
    - from: internal/sources/window.go
      to: internal/tui/view.go
      via: "WindowKind row lookup"
      pattern: "WindowSonnetSevenDay"
---

<objective>
Add optional Claude Sonnet-only weekly quota support and refactor quota row rendering so right-side percent/reset text aligns cleanly at normal and compact terminal widths.

Purpose: Phase 7 starts by making the row set and row layout stable before adding source-level freshness lines in Plan 02.
Output: Optional Sonnet weekly source support, a persistent `Sonnet 7d` placeholder row, fixed-width quota row columns, and deterministic render/parser tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-row-alignment-claude-sonnet-limit-and-source-freshness/07-CONTEXT.md
@.planning/phases/07-row-alignment-claude-sonnet-limit-and-source-freshness/07-RESEARCH.md
@.planning/phases/07-row-alignment-claude-sonnet-limit-and-source-freshness/07-UI-SPEC.md
@.planning/phases/07-row-alignment-claude-sonnet-limit-and-source-freshness/07-PATTERNS.md
@internal/sources/window.go
@internal/sources/claude.go
@internal/sources/claude_test.go
@internal/install/claude_hook.go
@internal/install/claude_hook_test.go
@internal/tui/view.go
@internal/tui/view_test.go
</context>

<threat_model>
Scope: Local file parsing and terminal rendering only; no network, OAuth, Keychain, daemon, or credential reads are introduced.

Potential threats:
- Low: Parser error messages could expose raw Claude cache JSON details. Mitigation: keep existing typed `SourceError` categories concise and do not render raw JSON or raw source categories.
- Low: Optional Sonnet field handling could reject otherwise valid Claude quota data. Mitigation: required-window validation remains strict only for `five_hour`, `seven_day`, and `written_at`; invalid optional Sonnet fields are skipped.
- Low: Maliciously wide or styled terminal output could wrap in small panes. Mitigation: all rendered lines are checked with ANSI stripping and Lip Gloss width assertions at 80, 50, 49, 30, 29, and 20 columns.

Block-on severity: high. No high-severity threats are expected for this local-only rendering change.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add optional Claude Sonnet weekly source support</name>
  <files>internal/sources/window.go, internal/sources/claude.go, internal/sources/claude_test.go, internal/install/claude_hook.go, internal/install/claude_hook_test.go</files>
  <read_first>internal/sources/window.go, internal/sources/claude.go, internal/sources/claude_test.go, internal/install/claude_hook.go, internal/install/claude_hook_test.go, .planning/phases/07-row-alignment-claude-sonnet-limit-and-source-freshness/07-CONTEXT.md, .planning/phases/07-row-alignment-claude-sonnet-limit-and-source-freshness/07-RESEARCH.md</read_first>
  <action>Add `WindowSonnetSevenDay WindowKind = "sonnet_seven_day"` in `internal/sources/window.go`. Extend the Claude cache reader with optional fields for recognized Sonnet weekly keys: canonical `sonnet_seven_day` and compatibility alias `sonnet_weekly`. Keep `five_hour`, `seven_day`, and `written_at` required. If a recognized optional Sonnet field is present and its `used_percentage` and `resets_at` are valid, append `cacheWindow.window(WindowSonnetSevenDay, "Sonnet 7d", writtenAt, stale, staleAge)` after `Claude 7d`; if the optional field is absent or malformed, return only the two required Claude windows without error. Extend the statusline cache writer to accept the same incoming optional names and write only canonical JSON key `sonnet_seven_day` when valid.</action>
  <verify>go test ./internal/sources ./internal/install</verify>
  <acceptance_criteria>
    - `internal/sources/window.go` contains `WindowSonnetSevenDay WindowKind = "sonnet_seven_day"`.
    - `internal/sources/claude.go` contains `sonnet_seven_day` and `sonnet_weekly`.
    - `internal/sources/claude_test.go` has a test case where a valid `sonnet_seven_day` cache returns three Claude windows and the third window has label `Sonnet 7d`.
    - `internal/sources/claude_test.go` has a test case where no Sonnet field still returns exactly `Claude 5h` and `Claude 7d`.
    - `internal/sources/claude_test.go` has a test case where malformed optional Sonnet data does not return `ErrorMalformed`.
    - `internal/install/claude_hook_test.go` proves statusline input with optional Sonnet weekly data writes JSON containing `sonnet_seven_day`.
    - `internal/install/claude_hook_test.go` proves statusline input without optional Sonnet data does not require or write `sonnet_seven_day`.
  </acceptance_criteria>
  <done>Claude source and cache-writer tests cover present, absent, and malformed optional Sonnet weekly data while preserving required Claude windows.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor quota rows to fixed columns and persistent Sonnet placeholder</name>
  <files>internal/tui/view.go, internal/tui/view_test.go</files>
  <read_first>internal/tui/view.go, internal/tui/view_test.go, internal/sources/window.go, .planning/phases/07-row-alignment-claude-sonnet-limit-and-source-freshness/07-UI-SPEC.md, .planning/phases/07-row-alignment-claude-sonnet-limit-and-source-freshness/07-PATTERNS.md</read_first>
  <action>Replace the hard-coded four-row list in `renderRows` with an ordered row spec list that renders Claude rows first as `Claude 5h`, `Claude 7d`, `Sonnet 7d`, then Codex rows as `Codex 5h`, `Codex 7d`. Use `WindowSonnetSevenDay` for the Sonnet lookup. Refactor row rendering around explicit layout constants from the UI spec: normal inner width `>= 46`, label width `9`, normal percent width `4`, normal reset width `7`, two-cell gaps, minimum progress width `6`; compact inner width `>= 26`, short label width `5`, percent preserved, reset shortened to hours where needed; very narrow width `< 26`, no bars and reset omitted if it does not fit. Implement a placeholder branch that uses the same columns as data rows and renders `Sonnet 7d` with placeholder marker `-` or the existing missing marker, placeholder percent, and placeholder reset.</action>
  <verify>go test ./internal/tui</verify>
  <acceptance_criteria>
    - `internal/tui/view.go` contains row spec entries for `Claude 5h`, `Claude 7d`, `Sonnet 7d`, `Codex 5h`, and `Codex 7d` in that order.
    - `internal/tui/view.go` references `sources.WindowSonnetSevenDay`.
    - `internal/tui/view_test.go` asserts startup output contains `Sonnet 7d` exactly once.
    - `internal/tui/view_test.go` asserts source-backed output renders a real `Sonnet 7d` row when a `WindowSonnetSevenDay` window is present.
    - `internal/tui/view_test.go` verifies normal-width right-column alignment for rows containing `0h 54m`, `21h 1m`, and `100%`.
    - `internal/tui/view_test.go` checks line widths at 80, 50, 49, 30, 29, and 20 columns after ANSI stripping.
    - `internal/tui/view_test.go` asserts width 29 omits bars if bars would crowd percent/reset text.
  </acceptance_criteria>
  <done>Quota rendering includes the Sonnet row and fixed readable columns across normal, compact, and very narrow widths.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `go test ./internal/sources ./internal/install ./internal/tui` passes.
- [ ] `go test ./...` passes.
- [ ] Render tests prove `Sonnet 7d` placeholder and real data behavior.
- [ ] Render tests prove fixed right-column alignment and width safety.
</verification>

<success_criteria>
- CLD-05 is covered by source support and real `Sonnet 7d` rendering when local Claude cache exposes a valid optional Sonnet weekly limit.
- CLD-06 is covered by the persistent `Sonnet 7d` placeholder when optional Sonnet data is missing or malformed.
- POL-01 is covered by fixed percent/reset columns in normal-width quota rows.
- POL-03 is partially covered by row-width tests at 80, 50, 49, 30, 29, and 20 columns; Plan 02 completes freshness-line coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/07-row-alignment-claude-sonnet-limit-and-source-freshness/07-01-SUMMARY.md`.
</output>

---
phase: 07-row-alignment-claude-sonnet-limit-and-source-freshness
plan: "02"
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - internal/tui/view.go
  - internal/tui/view_test.go
  - internal/tui/update_test.go
autonomous: true
requirements:
  - POL-02
  - POL-03
  - POL-04
user_setup: []
must_haves:
  truths:
    - "D-09: `renderRows` places one Claude source freshness line immediately after `Claude 5h`, `Claude 7d`, and `Sonnet 7d`, and one Codex source freshness line immediately after `Codex 5h` and `Codex 7d`."
    - "D-10: Normal-width freshness text includes source identity and absolute time shaped like `Claude updated 2:14 PM` and `Codex updated 2:14 PM`."
    - "D-11: When width allows, freshness text includes both absolute time and relative age shaped like `Claude updated 2:14 PM (3m ago)`."
    - "D-12: Compact freshness text keeps source identity, for example `Claude 2:14 PM`; very narrow text may use `Cl 2:14` and `Cx 2:14`."
    - "D-13: If a refresh fails while last-known-good rows remain visible, the affected source freshness line includes the current-error hint."
    - "D-14: Current source refresh failures render the exact concise wording `refresh failed`."
    - "D-15: If stale age and current refresh failure both apply, the freshness line can render combined status such as `Claude updated 2:14 PM (2h old, refresh failed)` when width allows."
    - "D-16: Footer recovery hints remain only for first-run missing data; once source rows exist, stale/current refresh status appears on source freshness lines instead of duplicated footer warnings."
  artifacts:
    - path: internal/tui/view.go
      provides: "Source freshness and current refresh-failure rendering"
      contains: "refresh failed"
    - path: internal/tui/view_test.go
      provides: "Freshness, stale, current-error, and responsive render coverage"
      contains: "Claude updated"
    - path: internal/tui/update_test.go
      provides: "Last-known-good refresh failure state coverage"
      contains: "last-known-good"
  key_links:
    - from: internal/tui/update.go
      to: internal/tui/view.go
      via: "Model.errors consumed for source-level freshness status"
      pattern: "errors\\["
    - from: internal/tui/view.go
      to: internal/tui/view_test.go
      via: "deterministic clock controls absolute and relative freshness text"
      pattern: "WithClock"
---

<objective>
Add source-level freshness rows and current refresh-failure hints below each provider group while preserving last-known-good quota rows and compact pane readability.

Purpose: Complete Phase 7 by moving stale/current source status into the quota display where it is visible with existing rows, while keeping footer hints focused on first-run recovery.
Output: Claude and Codex freshness lines, concise `refresh failed` source status, footer deduplication, and deterministic tests across the required widths.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-row-alignment-claude-sonnet-limit-and-source-freshness/07-CONTEXT.md
@.planning/phases/07-row-alignment-claude-sonnet-limit-and-source-freshness/07-RESEARCH.md
@.planning/phases/07-row-alignment-claude-sonnet-limit-and-source-freshness/07-UI-SPEC.md
@.planning/phases/07-row-alignment-claude-sonnet-limit-and-source-freshness/07-PATTERNS.md
@.planning/phases/07-row-alignment-claude-sonnet-limit-and-source-freshness/07-01-SUMMARY.md
@internal/tui/view.go
@internal/tui/view_test.go
@internal/tui/update.go
@internal/tui/update_test.go
</context>

<threat_model>
Scope: Terminal rendering of already-local quota source status. No new data source, network access, credential read, or background process is introduced.

Potential threats:
- Low: Source error categories such as `malformed`, `read_error`, or `no_usable_event` could leak into the TUI. Mitigation: map all current source errors with visible rows to the literal text `refresh failed`.
- Low: Stale status and refresh failure could duplicate in footer and freshness lines, making the display confusing. Mitigation: footer hints only render when a source has no rows; visible rows carry source-level status.
- Low: Freshness lines could wrap in compact tmux panes. Mitigation: width-specific copy variants and line-width assertions at 80, 50, 49, 30, 29, and 20 columns.

Block-on severity: high. No high-severity threats are expected for this local-only rendering change.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Render source freshness lines under provider groups</name>
  <files>internal/tui/view.go, internal/tui/view_test.go</files>
  <read_first>internal/tui/view.go, internal/tui/view_test.go, internal/tui/update.go, .planning/phases/07-row-alignment-claude-sonnet-limit-and-source-freshness/07-UI-SPEC.md, .planning/phases/07-row-alignment-claude-sonnet-limit-and-source-freshness/07-PATTERNS.md, .planning/phases/07-row-alignment-claude-sonnet-limit-and-source-freshness/07-01-SUMMARY.md</read_first>
  <action>Update `renderRows` so the output order is Claude quota rows, Claude freshness line, Codex quota rows, Codex freshness line. Add helper functions in `internal/tui/view.go` to compute source freshness from `m.windows[product]`: use the first or latest non-zero `CapturedAt`, compute relative age from `StaleAge` when positive or from `now.Sub(CapturedAt)`, clamp negative age to zero, and format absolute local time with Go layout `3:04 PM`. At normal width, render `Claude updated 2:14 PM` and include relative age as `Claude updated 2:14 PM (3m ago)` when it fits. At compact widths, render `Claude 2:14 PM` / `Codex 2:14 PM`. At very narrow widths, render `Cl 2:14` / `Cx 2:14` if that fits. Style freshness text with the existing hint/footer color rather than adding a new palette color.</action>
  <verify>go test ./internal/tui</verify>
  <acceptance_criteria>
    - `internal/tui/view.go` contains `Claude updated` and `Codex updated`.
    - `internal/tui/view.go` derives freshness from `CapturedAt` and `StaleAge`.
    - `internal/tui/view_test.go` asserts `Claude updated 2:14 PM` appears below Claude rows at normal width.
    - `internal/tui/view_test.go` asserts `Codex updated 2:14 PM` appears below Codex rows at normal width.
    - `internal/tui/view_test.go` asserts freshness lines include relative age text like `3m ago` or `2h old` when width allows.
    - `internal/tui/view_test.go` asserts compact freshness text keeps source identity at widths 49 and 30.
    - `internal/tui/view_test.go` asserts all rendered lines stay within widths 80, 50, 49, 30, 29, and 20 after ANSI stripping.
  </acceptance_criteria>
  <done>Each source group has one readable freshness line at normal, compact, and very narrow widths.</done>
</task>

<task type="auto">
  <name>Task 2: Move current refresh-failure hints onto freshness lines</name>
  <files>internal/tui/view.go, internal/tui/view_test.go, internal/tui/update_test.go</files>
  <read_first>internal/tui/view.go, internal/tui/view_test.go, internal/tui/update.go, internal/tui/update_test.go, .planning/phases/07-row-alignment-claude-sonnet-limit-and-source-freshness/07-CONTEXT.md, .planning/phases/07-row-alignment-claude-sonnet-limit-and-source-freshness/07-UI-SPEC.md</read_first>
  <action>Use the existing `m.errors[product]` state preserved by `mergeRefresh` to append source-level current-error status to freshness lines when that product still has rows. Render the literal text `refresh failed` for any current error category and never render raw category values. When stale age and current refresh failure both apply and normal width allows it, render a combined parenthetical status such as `Claude updated 2:14 PM (2h old, refresh failed)`. Change `footerRecoveryHints` so stale/current-error hints are not duplicated in the footer once `hasWindows(m, product)` is true; keep `Claude: run install-claude-hook`, `Claude: open Claude`, and `Codex: open Codex` only for source errors when the affected source has no rows.</action>
  <verify>go test ./internal/tui</verify>
  <acceptance_criteria>
    - `internal/tui/view.go` contains the exact string `refresh failed`.
    - `internal/tui/view.go` does not render raw error categories `malformed`, `read_error`, or `no_usable_event`.
    - `internal/tui/view_test.go` asserts a Claude current error with preserved Claude windows renders `refresh failed` on the Claude freshness line.
    - `internal/tui/view_test.go` asserts a stale Claude window with a current error can render `2h old, refresh failed` when width allows.
    - `internal/tui/view_test.go` asserts footer output does not contain `Claude data 2h old; open Claude` once Claude rows exist.
    - `internal/tui/view_test.go` asserts first-run missing data still renders `Claude: run install-claude-hook`, `Claude: open Claude`, or `Codex: open Codex` as appropriate.
    - `internal/tui/update_test.go` keeps or extends the last-known-good refresh failure test proving windows are preserved and `m.errors[product]` remains set after a later source failure.
  </acceptance_criteria>
  <done>Current source refresh failures are visible beside preserved rows without duplicating stale/recovery hints in the footer.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `go test ./internal/tui` passes.
- [ ] `go test ./...` passes.
- [ ] Render tests prove freshness rows under both source groups.
- [ ] Render tests prove `refresh failed` appears for preserved last-known-good rows and raw error categories do not appear.
- [ ] Width assertions pass at 80, 50, 49, 30, 29, and 20 columns.
</verification>

<success_criteria>
- POL-02 is covered by one last-refreshed date/time line under Claude rows and one under Codex rows.
- POL-03 is covered by freshness and row line-width tests at normal, narrow, and very narrow terminal widths.
- POL-04 is covered by a concise source-level `refresh failed` hint while last-known-good rows remain visible.
</success_criteria>

<output>
After completion, create `.planning/phases/07-row-alignment-claude-sonnet-limit-and-source-freshness/07-02-SUMMARY.md`.
</output>
